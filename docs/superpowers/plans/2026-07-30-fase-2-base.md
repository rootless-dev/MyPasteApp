# Fase 2 — Base: plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconstruir a janela de Settings, parar de destruir texto formatado na
captura, e dar ao item de histórico um menu de contexto, um editor, um rótulo,
um caminho de criação manual e um painel de preview.

**Architecture:** Oito entregas em sequência sobre `develop`. A fundação
(`PreferenceKeys`) vem primeiro porque todo o resto grava preferências; o schema
(texto rico) vem cedo para que nada seja escrito sobre o modelo antigo; o menu
de contexto vira o chassi onde as ações das entregas seguintes se penduram. A
lógica nova sai das views para tipos puros (`RichText`, `RetentionWindow`,
`ItemActions`), no mesmo padrão que a Fase 1 estabeleceu com
`ClipboardMonitor.shouldCapture` — é o que torna testável um app cuja suíte não
alcança AppKit.

**Tech Stack:** Swift 5, SwiftUI, AppKit, SwiftData, Swift Testing.
Xcode 26.6, SDK macOS 26.5, deployment target 26.2.

**Spec:** `docs/superpowers/specs/2026-07-30-fase-2-base-design.md`

## Global Constraints

- **Branch:** `feature/fase-2-base`, criada a partir de `develop`.
- **Commits autorizados nesta branch.** Carlos autorizou em 2026-07-30 que os
  subagentes commitem em `feature/fase-2-base` sem pedir OK a cada tarefa, já
  que este plano foi revisado e aprovado por ele e cada tarefa traz a mensagem
  pronta. A autorização **não** cobre `develop`, `main`, `git push` nem abrir
  PR — nada disso acontece sem pedido explícito dele.
- **Commitar apenas os arquivos da própria tarefa.** Nunca `git add -A` nem
  `git add .`: há documentos não versionados na árvore (`ROADMAP.md`,
  `DESIGN.md`, `design-refs/`) e a spec e o plano desta fase, cujo versionamento
  é decisão separada do Carlos. Liste os caminhos explicitamente.
- Mensagens de commit em **inglês**, seguindo Conventional Commits.
- **Os valores das strings de `@AppStorage` nunca mudam.** Renomear um valor
  apaga a configuração de qualquer instalação existente, em silêncio.
- Comentários e nomes de código em inglês, seguindo o que já existe no
  repositório. Rótulos de interface visíveis ao usuário seguem o idioma já
  usado em cada view (o card está em português, `PreferencesView` em inglês) —
  nenhuma tradução em massa faz parte desta fase.
- `PBXFileSystemSynchronizedRootGroup`: arquivos novos entram no alvo por
  existirem no diretório. Não há `project.pbxproj` para editar.
- **Comando de teste completo:**
  ```bash
  set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
    -project MyPasteApp.xcodeproj -scheme MyPasteApp \
    -configuration Debug -destination 'platform=macOS'
  ```
- **Comando de teste de uma suíte:** acrescente
  `-only-testing:MyPasteAppTests/<NomeDaSuite>`.
- Testes cobrem **lógica pura**. Nada em `Views/` ou `Window/` tem teste
  automatizado — essas entregas terminam com verificação manual, listada no fim
  deste plano.

---

## Estrutura de arquivos

**Criados:**

| Arquivo | Responsabilidade |
|---|---|
| `Services/PreferenceKeys.swift` | Constantes das chaves de `UserDefaults` |
| `Services/RetentionWindow.swift` | As paradas do slider e o mapeamento valor ↔ parada |
| `Services/RichText.swift` | Que tipo do pasteboard guardar; o que escrever ao colar |
| `Services/ItemActions.swift` | As ações sobre um item, sem interface |
| `Views/Preferences/SettingsView.swift` | `NavigationSplitView` e a enumeração de seções |
| `Views/Preferences/GeneralSettingsView.swift` | Login, som, bloco "Colar itens" |
| `Views/Preferences/HistorySettingsView.swift` | Retenção, máximo, preview, apagar |
| `Views/Preferences/AppearanceSettingsView.swift` | Densidade, previews de link, números |
| `Views/Preferences/ShortcutsSettingsView.swift` | As duas hotkeys e o conflito |
| `Views/Preferences/PrivacySettingsView.swift` | Compartilhamento, marcadores, apps |
| `Views/Preferences/RetentionSlider.swift` | O slider com paradas nomeadas |
| `Views/Preferences/SettingsToggle.swift` | Toggle com linha de descrição |
| `Views/ItemContextMenu.swift` | O menu de contexto do card |
| `Views/RichTextEditor.swift` | `NSViewRepresentable` sobre `NSTextView` |
| `Views/ItemEditorView.swift` | Campo de rótulo, corpo, salvar/cancelar |
| `Window/ItemEditorWindowController.swift` | A janela do editor |
| `Window/ItemPreviewPanel.swift` | O painel ancorado do preview |
| `Views/ItemPreviewView.swift` | Conteúdo do preview por tipo |

**Removido:** `Views/PreferencesView.swift`.

**Modificados:** `Models/ClipboardItem.swift`, `Services/ClipboardMonitor.swift`,
`Services/ClipboardWriter.swift`, `Services/RetentionPolicy.swift`,
`Services/HotkeyManager.swift`, `Views/OverlayView.swift`,
`Views/ClipboardCardView.swift`, `AppDelegate.swift`.

**Testes criados:** `PreferenceKeysTests.swift`, `RetentionWindowTests.swift`,
`RichTextReadTests.swift`, `RichTextWriteTests.swift`, `ItemEditTests.swift`,
`ManualItemTests.swift`, `ItemSearchTests.swift`, `PreviewTriggerTests.swift`.

**Teste modificado:** `RetentionPolicyTests.swift` (ver Task 4 — há um teste
existente que contradiz a spec).

---

## Duas descobertas feitas ao escrever este plano

**1. Existe um teste que afirma o contrário da spec.**
`RetentionPolicyTests.nonPositiveLimits` (linha 73) testa explicitamente que
`retentionDays = 0` cai para 30, com o comentário "Zero would otherwise wipe the
entire history on the next prune." Está correto para o comportamento atual: com
`retentionDays = 0` o corte fica em "agora" e `prune()` apagaria tudo. A spec
inverte a semântica de `0` para "Para Sempre", que é seguro. O teste **precisa
ser dividido**, não apenas alterado: `maxItems = 0` continua caindo para 500.
Isso está na Task 4.

**2. `AppColorExtractor` e `ClipboardCardView` não precisam mudar.** A spec diz
que o item criado à mão fica com `sourceAppBundleID = nil` e que os dois tipos
ganham um caso para nulo. Ao escrever a Task 19 ficou claro que gravar
`Bundle.main.bundleIdentifier` entrega o mesmo resultado visual sem código novo
em lugar nenhum: `AppColorExtractor` extrai a cor do ícone do MyPasteApp e
`ClipboardCardView.appIcon` resolve o ícone, ambos pelo caminho que já existe. E
é semanticamente verdadeiro — a origem do item **é** este app. Ganho adicional:
o filtro por app do item 12 (Fase 3) passa a listá-lo corretamente em vez de
precisar de um grupo "Desconhecido". O desvio está registrado na Task 19.

---

## Task 1: `PreferenceKeys` e o teste de chaves congeladas

**Files:**
- Create: `MyPasteApp/Services/PreferenceKeys.swift`
- Create: `MyPasteAppTests/PreferenceKeysTests.swift`

**Interfaces:**
- Produces: `enum PreferenceKeys` com uma `static let` `String` por chave. Todas
  as tarefas seguintes referenciam estas constantes em vez de literais.

Esta é a defesa contra o modo de falha mais caro da fase: uma chave renomeada
não quebra a compilação nem falha um teste comum — o app simplesmente esquece a
configuração do usuário, sem erro.

- [ ] **Step 1: Escrever o teste que congela os valores**

`MyPasteAppTests/PreferenceKeysTests.swift`:

```swift
//
//  PreferenceKeysTests.swift
//  MyPasteAppTests
//

import Testing

@testable import MyPasteApp

/// Freezes the on-disk names of every preference key.
///
/// These strings are a storage format, not an implementation detail: renaming
/// one makes every existing installation silently fall back to its default,
/// with no crash and no failing build to catch it. Changing a value here means
/// changing what users already have on disk, so this test is meant to be hard
/// to update by accident.
@Suite("Preference keys")
struct PreferenceKeysTests {
    @Test("Key names never change")
    func keysAreFrozen() {
        #expect(PreferenceKeys.maxItems == "maxItems")
        #expect(PreferenceKeys.retentionDays == "retentionDays")
        #expect(PreferenceKeys.previewTextLength == "previewTextLength")
        #expect(PreferenceKeys.enableSoundFeedback == "enableSoundFeedback")
        #expect(PreferenceKeys.ignoredAppsRaw == "ignoredAppsRaw")
        #expect(PreferenceKeys.autoPasteEnabled == "autoPasteEnabled")
        #expect(PreferenceKeys.pasteDelayMs == "pasteDelayMs")
        #expect(PreferenceKeys.showLinkPreviews == "showLinkPreviews")
        #expect(PreferenceKeys.cardDensity == "cardDensity")
        #expect(PreferenceKeys.showQuickPasteNumbers == "showQuickPasteNumbers")
        #expect(PreferenceKeys.showInScreenSharing == "showInScreenSharing")
        #expect(PreferenceKeys.ignoreConcealedContent == "ignoreConcealedContent")
        #expect(PreferenceKeys.ignoreTransientContent == "ignoreTransientContent")
        #expect(PreferenceKeys.ignoreAutoGeneratedContent == "ignoreAutoGeneratedContent")
        #expect(PreferenceKeys.alwaysPastePlainText == "alwaysPastePlainText")
    }

    @Test("No two keys collide")
    func keysAreUnique() {
        let all = PreferenceKeys.all
        #expect(Set(all).count == all.count)
    }
}
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:MyPasteAppTests/PreferenceKeysTests
```

Esperado: falha de compilação, `cannot find 'PreferenceKeys' in scope`.

- [ ] **Step 3: Criar o tipo**

`MyPasteApp/Services/PreferenceKeys.swift`:

```swift
//
//  PreferenceKeys.swift
//  MyPasteApp
//

import Foundation

/// The `UserDefaults` keys the app reads and writes.
///
/// These values are a storage format. Renaming one doesn't break the build or
/// any behaviour that a test would notice — it just makes every existing
/// installation fall back to defaults, losing the user's configuration in
/// silence. `PreferenceKeysTests` freezes them for that reason.
///
/// `KeyCombo` keeps its own two keys (`KeyCombo.storageKey` and
/// `KeyCombo.pauseStorageKey`): they're already named constants next to the
/// type that owns their format, and moving them here would only add a hop.
enum PreferenceKeys {
    static let maxItems = "maxItems"
    static let retentionDays = "retentionDays"
    static let previewTextLength = "previewTextLength"
    static let enableSoundFeedback = "enableSoundFeedback"
    static let ignoredAppsRaw = "ignoredAppsRaw"
    static let autoPasteEnabled = "autoPasteEnabled"
    static let pasteDelayMs = "pasteDelayMs"
    static let showLinkPreviews = "showLinkPreviews"
    static let cardDensity = "cardDensity"
    static let showQuickPasteNumbers = "showQuickPasteNumbers"
    static let showInScreenSharing = "showInScreenSharing"
    static let ignoreConcealedContent = "ignoreConcealedContent"
    static let ignoreTransientContent = "ignoreTransientContent"
    static let ignoreAutoGeneratedContent = "ignoreAutoGeneratedContent"

    /// Forces every paste to plain text, regardless of what was captured.
    /// Off by default: the app should hand back what it was given.
    static let alwaysPastePlainText = "alwaysPastePlainText"

    static let all: [String] = [
        maxItems, retentionDays, previewTextLength, enableSoundFeedback,
        ignoredAppsRaw, autoPasteEnabled, pasteDelayMs, showLinkPreviews,
        cardDensity, showQuickPasteNumbers, showInScreenSharing,
        ignoreConcealedContent, ignoreTransientContent,
        ignoreAutoGeneratedContent, alwaysPastePlainText,
    ]
}
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

Mesmo comando do Step 2. Esperado: PASS, 2 testes.

- [ ] **Step 5: Migrar os consumidores para as constantes**

Substituir o literal pela constante, **sem alterar nenhum valor**:

- `Services/ClipboardMonitor.swift` — linhas 226, 231, 235, 241
- `Services/RetentionPolicy.swift` — linhas 15, 20
- `Services/PasteboardPrivacy.swift` — linhas 35, 36, 37
- `Services/WindowPrivacy.swift` — a leitura de `showInScreenSharing`
- `Views/OverlayView.swift` — linha 17
- `Views/ClipboardCardView.swift` — linha 15
- `Window/OverlayWindowController.swift` — linhas 78 e 80
- `Views/PreferencesView.swift` — linhas 13 a 26

`@AppStorage` aceita a constante diretamente: `@AppStorage(PreferenceKeys.maxItems)`.

- [ ] **Step 6: Rodar a suíte inteira**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS, 95 testes anteriores mais os 2 novos. **Qualquer** teste que
falhe aqui indica um valor de chave alterado por engano — investigue antes de
seguir.

- [ ] **Step 7: Commit**

Mensagem sugerida:

```
refactor(preferences): centralize UserDefaults keys in one type

The key strings are a storage format: renaming one silently drops every
existing user's configuration, with no crash and no failing build. Freeze
them in a test.
```

---

## Task 2: `HotkeyManager` propaga a falha de registro

**Files:**
- Modify: `MyPasteApp/Services/HotkeyManager.swift:56-76`
- Modify: `MyPasteApp/AppDelegate.swift`

**Interfaces:**
- Consumes: nada da Task 1.
- Produces: `@discardableResult func register(combo: KeyCombo? = nil) -> Bool` —
  `true` quando `RegisterEventHotKey` devolveu `noErr`. A Task 3 consome isso.

Pendência herdada da Fase 1: o `OSStatus` é apenas logado e
`pauseHotkeyRegistered` é marcado `true` sem checá-lo, então o menu pode
anunciar um atalho morto.

- [ ] **Step 1: Fazer `register` devolver o resultado**

Em `HotkeyManager.swift`, trocar a assinatura da linha 56 e o bloco 70-75:

```swift
    /// Registers the shortcut, returning whether it actually took effect.
    ///
    /// The result matters to the caller: when another app already owns the
    /// combination, `RegisterEventHotKey` fails and the shortcut simply never
    /// fires. Advertising it in a menu at that point tells the user about a
    /// key that does nothing.
    @discardableResult
    func register(combo: KeyCombo? = nil) -> Bool {
        let combo = combo ?? storedCombo
        unregister()

        Self.installSharedHandlerIfNeeded()
        Self.instances[id.rawValue] = self

        let hotKeyID = EventHotKeyID(signature: Self.signature, id: id.rawValue)
        let status = RegisterEventHotKey(combo.keyCode,
                                         combo.carbonModifiers,
                                         hotKeyID,
                                         GetApplicationEventTarget(),
                                         0,
                                         &hotKeyRef)
        guard status == noErr else {
            // Most often another app already owns the combination.
            NSLog("Failed to register hotkey \(id) (\(combo.displayString)): OSStatus \(status)")
            Self.instances.removeValue(forKey: id.rawValue)
            return false
        }
        return true
    }
```

A remoção de `instances` no caminho de falha é intencional: sem ela, um manager
que falhou continuaria no dicionário, ocupando o slot do seu `ID`.

- [ ] **Step 2: Fazer o `AppDelegate` refletir o resultado real**

Localizar em `AppDelegate.swift` o ponto onde `pauseHotkeyRegistered` é marcado
`true` (dentro de `registerHotkeysCheckingConflict`) e passar a atribuir o
retorno de `register()` em vez do literal. O mesmo vale para a hotkey da
overlay, que precisa de um `overlayHotkeyRegistered` equivalente — a Task 3
consome os dois.

- [ ] **Step 3: Compilar e rodar a suíte**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS. Nenhum teste novo — o caminho de falha depende de outro
processo ser dono da combinação, o que a suíte não consegue montar. A cobertura
real é a verificação manual 2 no fim deste plano.

- [ ] **Step 4: Commit**

```
fix(hotkeys): report registration failures to the caller

RegisterEventHotKey's OSStatus was only logged, and the pause shortcut was
marked as registered without checking it. When another app owns the
combination, the menu advertised a shortcut that never fires.
```

---

## Task 3: O menu não anuncia atalho não registrado

**Files:**
- Modify: `MyPasteApp/AppDelegate.swift:193-215`

**Interfaces:**
- Consumes: `register() -> Bool` e as flags `overlayHotkeyRegistered` /
  `pauseHotkeyRegistered` da Task 2.

- [ ] **Step 1: Condicionar o sufixo do "Show history"**

Em `showStatusMenu()`, o item é montado hoje como:

```swift
let show = NSMenuItem(title: "Show history  \(KeyCombo.stored.displayString)", ...)
```

Passa a seguir o padrão que os itens de pausa já usam com `comboSuffix`:

```swift
        // Only advertise the shortcut when it actually registered. Another app
        // owning the combination leaves it dead, and a menu that still shows it
        // sends the user looking for a bug in the wrong place.
        let showSuffix = overlayHotkeyRegistered ? "  \(KeyCombo.stored.displayString)" : ""
        let show = NSMenuItem(title: "Show history\(showSuffix)",
                              action: #selector(toggleOverlay),
                              keyEquivalent: "")
```

- [ ] **Step 2: Compilar e rodar a suíte**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS.

- [ ] **Step 3: Commit**

```
fix(menu): hide the overlay shortcut when it isn't registered

Same class as the pause shortcut: the menu interpolated the combination
without knowing whether it took effect.
```

---

## Task 4: "Para Sempre" na retenção

**Files:**
- Modify: `MyPasteApp/Services/RetentionPolicy.swift:19-52`
- Modify: `MyPasteAppTests/RetentionPolicyTests.swift:73-80`

**Interfaces:**
- Consumes: `PreferenceKeys` (Task 1).
- Produces: `RetentionPolicy.retentionDays: Int?` — `nil` significa "não podar
  por idade". A Task 5 e a Task 8 dependem desta semântica.

**Atenção — este é o ponto mais delicado do plano.** A leitura atual trata `0`
como ausente, e a spec dá a `0` o significado oposto ("guardar para sempre").
Sem a mudança de leitura junto, gravar "Para Sempre" faria `prune()` voltar a 30
dias e apagar exatamente o histórico que o usuário mandou preservar.

- [ ] **Step 1: Dividir o teste que contradiz a spec**

Em `RetentionPolicyTests.swift`, **substituir** `nonPositiveLimits` (linhas
73-80) por três testes. `maxItems` mantém o comportamento antigo; `retentionDays`
muda:

```swift
    @Test("Non-positive maxItems falls back to the default", arguments: [0, -1])
    func nonPositiveMaxItems(value: Int) {
        // Zero would otherwise wipe the entire history on the next prune.
        defaults.store.set(value, forKey: PreferenceKeys.maxItems)
        #expect(policy.maxItems == 500)
    }

    @Test("A stored zero means keep forever")
    func zeroRetentionMeansForever() {
        // Deliberately the opposite of what maxItems does with zero. The
        // retention slider's last stop writes 0, and reading it as "unset"
        // would silently prune the history the user asked to keep.
        defaults.store.set(0, forKey: PreferenceKeys.retentionDays)
        #expect(policy.retentionDays == nil)
    }

    @Test("A negative retention is treated as unset")
    func negativeRetentionFallsBack() {
        defaults.store.set(-1, forKey: PreferenceKeys.retentionDays)
        #expect(policy.retentionDays == 30)
    }
```

- [ ] **Step 2: Escrever o teste de poda com "Para Sempre"**

Acrescentar à seção "Age based pruning" da mesma suíte:

```swift
    @Test("Keep forever skips the age pass entirely")
    func keepForeverSkipsAgePruning() throws {
        defaults.store.set(0, forKey: PreferenceKeys.retentionDays)
        insert(tag: "ancient", daysAgo: 9000)
        insert(tag: "fresh", daysAgo: 1)

        policy.prune()

        #expect(try remainingTags() == ["ancient", "fresh"])
    }

    @Test("Keep forever does not disable the maxItems cap")
    func keepForeverStillRespectsTheCap() throws {
        // Otherwise the store grows without bound: "forever" is about age, not
        // about quantity. The Settings screen says this out loud.
        defaults.store.set(0, forKey: PreferenceKeys.retentionDays)
        defaults.store.set(2, forKey: PreferenceKeys.maxItems)
        insert(tag: "newest", daysAgo: 0)
        insert(tag: "second", daysAgo: 1)
        insert(tag: "third", daysAgo: 2)

        policy.prune()

        #expect(try remainingTags() == ["newest", "second"])
    }
```

- [ ] **Step 3: Rodar e confirmar que falha**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:MyPasteAppTests/RetentionPolicyTests
```

Esperado: falha de compilação em `#expect(policy.retentionDays == nil)`, porque
`retentionDays` ainda é `Int` e não `Int?`.

- [ ] **Step 4: Trocar a leitura e a poda**

Em `RetentionPolicy.swift`, substituir a propriedade da linha 19 e o `prune()`:

```swift
    /// How many days to keep an item, or `nil` for "keep forever".
    ///
    /// Read through `object(forKey:)` rather than `integer(forKey:)` on
    /// purpose: the latter returns 0 both for a missing key and for a stored
    /// zero, and those two now mean opposite things — unset falls back to 30
    /// days, while a stored 0 is the slider's "Forever" stop. Reading them the
    /// same way would prune exactly the history the user asked to keep.
    var retentionDays: Int? {
        guard let stored = defaults.object(forKey: PreferenceKeys.retentionDays) as? Int else {
            return 30
        }
        if stored == 0 { return nil }
        return stored > 0 ? stored : 30
    }

    func prune() {
        // 1) Delete old non-pinned items — skipped entirely when the user
        //    asked to keep the history forever.
        if let days = retentionDays {
            let cutoff = Calendar.current.date(
                byAdding: .day, value: -days, to: .now
            ) ?? .now

            let oldDescriptor = FetchDescriptor<ClipboardItem>(
                predicate: #Predicate { !$0.isPinned && $0.createdAt < cutoff }
            )
            if let old = try? modelContext.fetch(oldDescriptor) {
                for item in old { modelContext.delete(item) }
            }
        }

        // 2) Cap at maxItems (keeps the most recent non-pinned ones). This
        //    runs even with "keep forever": that setting is about age, not
        //    about quantity, and without this the store grows without bound.
        let allDescriptor = FetchDescriptor<ClipboardItem>(
            predicate: #Predicate { !$0.isPinned },
            sortBy: [SortDescriptor(\.createdAt, order: .reverse)]
        )
        if let all = try? modelContext.fetch(allDescriptor), all.count > maxItems {
            for item in all[maxItems...] { modelContext.delete(item) }
        }

        try? modelContext.save()
    }
```

- [ ] **Step 5: Rodar e confirmar que passa**

Mesmo comando do Step 3. Esperado: PASS, incluindo os testes antigos
`defaultLimits` e `storedLimits`, que comparam `Int?` com literal e continuam
válidos sem alteração.

- [ ] **Step 6: Commit**

```
feat(retention): let the history be kept forever

A stored 0 now means "never prune by age", which is the opposite of what
integer(forKey:) reports for a missing key — hence the switch to
object(forKey:). The maxItems cap still applies: forever is about age, not
quantity.
```

---

## Task 5: `RetentionWindow` — as paradas do slider

**Files:**
- Create: `MyPasteApp/Services/RetentionWindow.swift`
- Create: `MyPasteAppTests/RetentionWindowTests.swift`

**Interfaces:**
- Produces: `enum RetentionWindow: CaseIterable` com `days: Int`, `label: String`,
  `RetentionWindow.nearest(toDays:) -> RetentionWindow` e
  `RetentionWindow(days:) -> RetentionWindow?`. A Task 7 (`RetentionSlider`)
  consome.

- [ ] **Step 1: Escrever os testes**

`MyPasteAppTests/RetentionWindowTests.swift`:

```swift
//
//  RetentionWindowTests.swift
//  MyPasteAppTests
//

import Testing

@testable import MyPasteApp

@Suite("Retention window")
struct RetentionWindowTests {
    @Test("The stops are ordered from shortest to forever")
    func stopsAreOrdered() {
        #expect(RetentionWindow.allCases.map(\.days) == [1, 7, 30, 365, 0])
    }

    @Test("Each stop maps to its stored value")
    func exactMatches() {
        #expect(RetentionWindow(days: 1) == .day)
        #expect(RetentionWindow(days: 7) == .week)
        #expect(RetentionWindow(days: 30) == .month)
        #expect(RetentionWindow(days: 365) == .year)
        #expect(RetentionWindow(days: 0) == .forever)
    }

    @Test("A value off the stops has no exact match")
    func noExactMatchForArbitraryValue() {
        #expect(RetentionWindow(days: 45) == nil)
    }

    @Test("An off-stop value is displayed at the closest stop",
          arguments: [(2, RetentionWindow.day), (5, .week), (20, .month),
                      (45, .month), (200, .year), (9000, .year)])
    func nearestStop(value: Int, expected: RetentionWindow) {
        // Display only — nothing is rewritten until the user drags the slider,
        // so an existing 45-day setting isn't silently rounded to 30.
        #expect(RetentionWindow.nearest(toDays: value) == expected)
    }

    @Test("Forever is never the nearest stop for a finite value")
    func foreverIsNotNearestForFiniteValues() {
        // Its stored value is 0, which would otherwise look like the closest
        // stop to any small number.
        #expect(RetentionWindow.nearest(toDays: 1) == .day)
        #expect(RetentionWindow.nearest(toDays: 0) == .forever)
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:MyPasteAppTests/RetentionWindowTests
```

Esperado: falha de compilação, `cannot find 'RetentionWindow' in scope`.

- [ ] **Step 3: Implementar**

`MyPasteApp/Services/RetentionWindow.swift`:

```swift
//
//  RetentionWindow.swift
//  MyPasteApp
//

import Foundation

/// The named stops of the retention slider.
///
/// The user thinks in durations, not in day counts, which is why the Settings
/// screen offers five stops instead of a number field. `forever` stores 0 —
/// see `RetentionPolicy.retentionDays` for why that value can't be read with
/// `integer(forKey:)`.
enum RetentionWindow: CaseIterable {
    case day
    case week
    case month
    case year
    case forever

    var days: Int {
        switch self {
        case .day:     return 1
        case .week:    return 7
        case .month:   return 30
        case .year:    return 365
        case .forever: return 0
        }
    }

    var label: String {
        switch self {
        case .day:     return "Day"
        case .week:    return "Week"
        case .month:   return "Month"
        case .year:    return "Year"
        case .forever: return "Forever"
        }
    }

    init?(days: Int) {
        guard let match = Self.allCases.first(where: { $0.days == days }) else {
            return nil
        }
        self = match
    }

    /// The stop to show the slider at for a value that isn't itself a stop —
    /// a setting of 45 days, for instance.
    ///
    /// Display only: nothing is written back until the user drags the slider,
    /// so an existing configuration is never silently rounded.
    ///
    /// `forever` is excluded from the search because its stored value is 0,
    /// which would otherwise measure as the closest stop to every small number.
    static func nearest(toDays days: Int) -> RetentionWindow {
        if days == 0 { return .forever }
        let finite = allCases.filter { $0 != .forever }
        return finite.min { abs($0.days - days) < abs($1.days - days) } ?? .month
    }
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Mesmo comando do Step 2. Esperado: PASS, 6 testes.

- [ ] **Step 5: Commit**

```
feat(retention): add the named stops of the retention slider
```

---

## Task 6: A janela de Settings — estrutura e navegação

**Files:**
- Create: `MyPasteApp/Views/Preferences/SettingsView.swift`
- Create: `MyPasteApp/Views/Preferences/SettingsToggle.swift`
- Modify: `MyPasteApp/AppDelegate.swift` (o `openPreferences`, linha 286)

**Interfaces:**
- Produces: `struct SettingsView: View` — substitui `PreferencesView` como raiz
  da janela. As Tasks 7 e 8 preenchem as seções.

Nesta tarefa a janela passa a existir com as cinco seções **vazias**; as views
de conteúdo entram nas duas tarefas seguintes. Dividir assim mantém cada passo
verificável: aqui se confere navegação e chrome, lá se confere cada controle.

- [ ] **Step 1: Criar o componente de toggle com descrição**

`MyPasteApp/Views/Preferences/SettingsToggle.swift`:

```swift
//
//  SettingsToggle.swift
//  MyPasteApp
//

import SwiftUI

/// A toggle with a line of explanation under it.
///
/// Every non-obvious preference gets one: a switch whose consequence isn't
/// visible from its label is a setting nobody understands without trying it.
struct SettingsToggle: View {
    let title: String
    let description: String
    @Binding var isOn: Bool

    var body: some View {
        VStack(alignment: .leading, spacing: 2) {
            Toggle(title, isOn: $isOn)
            Text(description)
                .font(.caption)
                .foregroundStyle(.secondary)
                .fixedSize(horizontal: false, vertical: true)
        }
    }
}
```

- [ ] **Step 2: Criar a janela com a sidebar**

`MyPasteApp/Views/Preferences/SettingsView.swift`:

```swift
//
//  SettingsView.swift
//  MyPasteApp
//

import SwiftUI

/// The Settings window: a sidebar of sections plus the selected section's
/// content.
///
/// A sidebar rather than a `TabView` because it scales: the roadmap adds
/// preferences for pinboards, per-app rules, OCR and backup, and tabs would be
/// cramped well before that. It's also what System Settings itself does.
struct SettingsView: View {
    enum Section: String, CaseIterable, Identifiable {
        case general, history, appearance, shortcuts, privacy

        var id: String { rawValue }

        var label: String {
            switch self {
            case .general:    return "General"
            case .history:    return "History"
            case .appearance: return "Appearance"
            case .shortcuts:  return "Shortcuts"
            case .privacy:    return "Privacy"
            }
        }

        var symbol: String {
            switch self {
            case .general:    return "gearshape"
            case .history:    return "clock"
            case .appearance: return "paintbrush"
            case .shortcuts:  return "keyboard"
            case .privacy:    return "hand.raised"
            }
        }
    }

    @State private var selection: Section = .general

    var body: some View {
        NavigationSplitView {
            List(Section.allCases, selection: $selection) { section in
                Label(section.label, systemImage: section.symbol)
                    .tag(section)
            }
            .navigationSplitViewColumnWidth(min: 160, ideal: 180, max: 220)
        } detail: {
            detail
                .navigationTitle(selection.label)
                .frame(minWidth: 420, minHeight: 400)
        }
    }

    @ViewBuilder
    private var detail: some View {
        switch selection {
        case .general:    Text("General")
        case .history:    Text("History")
        case .appearance: Text("Appearance")
        case .shortcuts:  Text("Shortcuts")
        case .privacy:    Text("Privacy")
        }
    }
}
```

Os cinco `Text` são andaimes desta tarefa e são substituídos nas Tasks 7 e 8.

- [ ] **Step 3: Apontar a janela para a nova raiz**

Em `AppDelegate.openPreferences`, trocar `PreferencesView()` por `SettingsView()`
no `NSHostingView`. **Não** remover `PreferencesView.swift` ainda: as Tasks 7 e
8 copiam de lá a lógica de hotkey e de limpar histórico, e apagar agora
obrigaria a recuperá-la do git.

Manter a chamada a `WindowPrivacy` que já existe nesse método — a janela de
Settings exibe a lista de apps ignorados, que é ela própria dado sensível.

- [ ] **Step 4: Compilar e rodar a suíte**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS. Sem testes novos — é view.

- [ ] **Step 5: Verificação manual mínima**

Abrir Preferências pelo menu da barra de status. Confirmar: a sidebar lista as
cinco seções, clicar em cada uma troca o conteúdo, o título muda, e a janela
pode ser redimensionada.

- [ ] **Step 6: Commit**

```
feat(settings): rebuild the window around a sidebar

Sections are still empty; the controls move over in the next commits.
```

---

## Task 7: Seções Geral, Histórico e Aparência

**Files:**
- Create: `MyPasteApp/Views/Preferences/GeneralSettingsView.swift`
- Create: `MyPasteApp/Views/Preferences/HistorySettingsView.swift`
- Create: `MyPasteApp/Views/Preferences/AppearanceSettingsView.swift`
- Create: `MyPasteApp/Views/Preferences/RetentionSlider.swift`
- Modify: `MyPasteApp/Views/Preferences/SettingsView.swift` (o `detail`)

**Interfaces:**
- Consumes: `PreferenceKeys` (Task 1), `RetentionWindow` (Task 5),
  `SettingsToggle` (Task 6).

- [ ] **Step 1: O slider de retenção**

`MyPasteApp/Views/Preferences/RetentionSlider.swift`:

```swift
//
//  RetentionSlider.swift
//  MyPasteApp
//

import SwiftUI

/// Retention as five named stops instead of a day count.
///
/// A value that isn't one of the stops (an existing 45-day setting) shows at
/// the closest one but is only rewritten when the user actually drags — no
/// configuration changes by itself just because the screen was opened.
struct RetentionSlider: View {
    @Binding var days: Int

    private var stops: [RetentionWindow] { RetentionWindow.allCases }

    private var index: Double {
        let window = RetentionWindow(days: days) ?? RetentionWindow.nearest(toDays: days)
        return Double(stops.firstIndex(of: window) ?? 0)
    }

    var body: some View {
        VStack(alignment: .leading, spacing: 4) {
            Slider(
                value: Binding(
                    get: { index },
                    set: { days = stops[Int($0.rounded())].days }
                ),
                in: 0...Double(stops.count - 1),
                step: 1
            )
            HStack {
                ForEach(stops, id: \.self) { stop in
                    Text(stop.label)
                        .font(.caption)
                        .foregroundStyle(.secondary)
                    if stop != stops.last { Spacer() }
                }
            }
            if RetentionWindow(days: days) == nil {
                Text("Currently set to \(days) days.")
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
        }
    }
}
```

- [ ] **Step 2: A seção Histórico**

`MyPasteApp/Views/Preferences/HistorySettingsView.swift`:

```swift
//
//  HistorySettingsView.swift
//  MyPasteApp
//

import SwiftData
import SwiftUI

struct HistorySettingsView: View {
    @Environment(\.modelContext) private var modelContext
    @AppStorage(PreferenceKeys.maxItems) private var maxItems: Int = 500
    @AppStorage(PreferenceKeys.retentionDays) private var retentionDays: Int = 30
    @AppStorage(PreferenceKeys.previewTextLength) private var previewTextLength: Int = 200

    var body: some View {
        Form {
            Section("Keep history") {
                RetentionSlider(days: $retentionDays)
                Text("Forever stops deleting by age. The maximum number of items below still applies, so the store can't grow without bound.")
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
            Section {
                Stepper("Maximum items: \(maxItems)",
                        value: $maxItems, in: 50...5000, step: 50)
                Stepper("Preview text length: \(previewTextLength)",
                        value: $previewTextLength, in: 80...500, step: 20)
            }
            Section {
                Button("Clear non-pinned history") { clearHistory() }
            }
        }
        .formStyle(.grouped)
    }

    /// Copied from the former `PreferencesView.clearHistory`.
    private func clearHistory() {
        let descriptor = FetchDescriptor<ClipboardItem>(
            predicate: #Predicate { !$0.isPinned }
        )
        if let items = try? modelContext.fetch(descriptor) {
            for item in items { modelContext.delete(item) }
            try? modelContext.save()
        }
    }
}
```

- [ ] **Step 3: A seção Geral**

`MyPasteApp/Views/Preferences/GeneralSettingsView.swift`:

```swift
//
//  GeneralSettingsView.swift
//  MyPasteApp
//

import ServiceManagement
import SwiftUI

struct GeneralSettingsView: View {
    @AppStorage(PreferenceKeys.enableSoundFeedback) private var enableSoundFeedback: Bool = true
    @AppStorage(PreferenceKeys.autoPasteEnabled) private var autoPasteEnabled: Bool = true
    @AppStorage(PreferenceKeys.pasteDelayMs) private var pasteDelayMs: Int = 50
    @AppStorage(PreferenceKeys.alwaysPastePlainText) private var alwaysPastePlainText: Bool = false
    @State private var launchAtLogin: Bool = SMAppService.mainApp.status == .enabled

    var body: some View {
        Form {
            Section {
                Toggle("Launch at login", isOn: $launchAtLogin)
                    .onChange(of: launchAtLogin) { _, newValue in
                        toggleLaunchAtLogin(newValue)
                    }
                Toggle("Sound on copy", isOn: $enableSoundFeedback)
            }
            Section("Paste items") {
                // A radio pair rather than a lone toggle: "auto-paste off"
                // required the user to deduce what happened instead.
                Picker("", selection: $autoPasteEnabled) {
                    Text("To the active app").tag(true)
                    Text("To the clipboard").tag(false)
                }
                .pickerStyle(.radioGroup)
                .labelsHidden()
                Text(autoPasteEnabled
                     ? "The selected item is pasted straight into the app you were using."
                     : "The selected item is copied to the clipboard for you to paste yourself.")
                    .font(.caption)
                    .foregroundStyle(.secondary)
                Stepper("Paste delay: \(pasteDelayMs) ms",
                        value: $pasteDelayMs, in: 0...500, step: 10)
                    .disabled(!autoPasteEnabled)
                Text("Increase the delay if some apps drop the simulated ⌘V.")
                    .font(.caption)
                    .foregroundStyle(.secondary)
                Divider()
                SettingsToggle(
                    title: "Always paste as plain text",
                    description: "Formatting is still recorded, but every paste hands over plain text. With this on, ⇧ changes nothing.",
                    isOn: $alwaysPastePlainText
                )
            }
        }
        .formStyle(.grouped)
    }

    /// Copied from the former `PreferencesView.toggleLaunchAtLogin`.
    private func toggleLaunchAtLogin(_ enabled: Bool) {
        do {
            if enabled {
                try SMAppService.mainApp.register()
            } else {
                try SMAppService.mainApp.unregister()
            }
        } catch {
            NSLog("Failed to toggle launch at login: \(error)")
        }
    }
}
```

- [ ] **Step 4: A seção Aparência**

`MyPasteApp/Views/Preferences/AppearanceSettingsView.swift`:

```swift
//
//  AppearanceSettingsView.swift
//  MyPasteApp
//

import SwiftUI

struct AppearanceSettingsView: View {
    @AppStorage(PreferenceKeys.cardDensity) private var cardDensity: String = CardDensity.comfortable.rawValue
    @AppStorage(PreferenceKeys.showLinkPreviews) private var showLinkPreviews: Bool = true
    @AppStorage(PreferenceKeys.showQuickPasteNumbers) private var showQuickPasteNumbers: Bool = true

    var body: some View {
        Form {
            Section {
                Picker("Card density", selection: $cardDensity) {
                    ForEach(CardDensity.allCases) { d in
                        Text(d.label).tag(d.rawValue)
                    }
                }
                SettingsToggle(
                    title: "Show link previews",
                    description: "Fetches the title, banner and favicon of copied links.",
                    isOn: $showLinkPreviews
                )
                SettingsToggle(
                    title: "Show quick paste numbers",
                    description: "⌘1–⌘9 paste the first nine visible cards. The shortcuts keep working with the numbers hidden.",
                    isOn: $showQuickPasteNumbers
                )
            }
        }
        .formStyle(.grouped)
    }
}
```

- [ ] **Step 5: Ligar as três no `detail` do `SettingsView`**

Substituir os andaimes correspondentes:

```swift
        case .general:    GeneralSettingsView()
        case .history:    HistorySettingsView()
        case .appearance: AppearanceSettingsView()
```

- [ ] **Step 6: Compilar e rodar a suíte**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS.

- [ ] **Step 7: Commit**

```
feat(settings): move general, history and appearance into the sidebar

Retention becomes a slider with named stops, including Forever, and
auto-paste becomes a labelled radio pair instead of a bare toggle.
```

---

## Task 8: Seções Atalhos e Privacidade, e a remoção da view antiga

**Files:**
- Create: `MyPasteApp/Views/Preferences/ShortcutsSettingsView.swift`
- Create: `MyPasteApp/Views/Preferences/PrivacySettingsView.swift`
- Modify: `MyPasteApp/Views/Preferences/SettingsView.swift`
- Delete: `MyPasteApp/Views/PreferencesView.swift`

**Interfaces:**
- Consumes: `PreferenceKeys` (Task 1), `SettingsToggle` (Task 6).

- [ ] **Step 1: A seção Atalhos**

Criar `ShortcutsSettingsView.swift` movendo **na íntegra** de
`PreferencesView.swift`: os `@State` `hotkey`, `pauseHotkey` e `hotkeyConflict`
(linhas 28-30), o bloco `Section("Global shortcuts")` (linhas 46-85), e os dois
métodos `refreshHotkeyState` (linhas 163-167) e `applyHotkeyChange` (linhas
203-218).

**Copiar os comentários de documentação junto.** Eles explicam por que
`applyHotkeyChange` é stateless e por que `refreshHotkeyState` re-deriva o
conflito a cada abertura — a janela é criada uma vez e reaproveitada, então um
`hotkeyConflict = true` obsoleto sobreviveria a todo fechamento. Sem esse
contexto, o próximo leitor "simplifica" o código de volta para o bug.

O corpo é o mesmo `Form` de hoje, com os dois `HStack` de `HotkeyRecorderView` e
os `onChange`, envolvido em `.formStyle(.grouped)`.

- [ ] **Step 2: A seção Privacidade**

`MyPasteApp/Views/Preferences/PrivacySettingsView.swift`:

```swift
//
//  PrivacySettingsView.swift
//  MyPasteApp
//

import SwiftUI

struct PrivacySettingsView: View {
    @AppStorage(PreferenceKeys.showInScreenSharing) private var showInScreenSharing: Bool = false
    @AppStorage(PreferenceKeys.ignoreConcealedContent) private var ignoreConcealedContent: Bool = true
    @AppStorage(PreferenceKeys.ignoreTransientContent) private var ignoreTransientContent: Bool = true
    @AppStorage(PreferenceKeys.ignoreAutoGeneratedContent) private var ignoreAutoGeneratedContent: Bool = false
    @AppStorage(PreferenceKeys.ignoredAppsRaw) private var ignoredAppsRaw: String = ""

    var body: some View {
        Form {
            Section {
                SettingsToggle(
                    title: "Show during screen sharing",
                    description: "When off, the overlay and this window don't appear in screen sharing or recordings — including screenshots you take yourself.",
                    isOn: $showInScreenSharing
                )
            }
            Section("Marked content") {
                Toggle("Ignore confidential content", isOn: $ignoreConcealedContent)
                Toggle("Ignore transient content", isOn: $ignoreTransientContent)
                Toggle("Ignore auto-generated content", isOn: $ignoreAutoGeneratedContent)
                Text("Apps can mark what they copy as a password, as throwaway data or as machine-generated. This only works when the source app marks it — and many don't, including Apple's own Passwords app. Those have to be listed below.")
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
            Section("Ignored apps") {
                TextEditor(text: $ignoredAppsRaw)
                    .font(.system(.body, design: .monospaced))
                    .frame(minHeight: 60, maxHeight: 100)
                    .overlay(
                        RoundedRectangle(cornerRadius: 4)
                            .stroke(Color.secondary.opacity(0.3))
                    )
                Text("One bundle ID per line. Nothing copied from these apps is ever recorded — this is the only protection that doesn't depend on the app marking its own content.")
                    .font(.caption)
                    .foregroundStyle(.secondary)
                Text("Worth adding: com.apple.Passwords · com.apple.keychainaccess")
                    .font(.caption)
                    .foregroundStyle(.secondary)
                    .textSelection(.enabled)
            }
        }
        .formStyle(.grouped)
    }
}
```

O `TextEditor` de bundle IDs permanece como está: substituí-lo pela lista com
ícones e `+`/`−` é o item 18, da Fase 5.

- [ ] **Step 3: Ligar as duas e apagar a view antiga**

Substituir os dois andaimes restantes no `detail`, e então
`git rm MyPasteApp/Views/PreferencesView.swift`.

- [ ] **Step 4: Compilar e rodar a suíte**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS. Se algo referenciar `PreferencesView`, o compilador aponta.

- [ ] **Step 5: Verificação manual — a mais importante da fase**

**Este é o `pronto quando` do item 6.** Abrir Settings e conferir **uma a uma**,
com as configurações que já existiam antes da reconstrução, que cada preferência
continua com o valor de antes e que alterá-la continua surtindo efeito:

máximo de itens · dias de retenção · tamanho do preview · som ao copiar ·
apagar histórico · atalho da overlay · atalho de pausa · aviso de conflito ·
densidade do card · previews de link · números do quick paste · colar no app
ativo · atraso da colagem · mostrar em compartilhamento de tela · os três
marcadores · lista de apps ignorados · abrir no login.

Um valor que apareça no padrão em vez do que estava configurado significa chave
errada — o modo de falha que esta fase inteira tenta evitar.

- [ ] **Step 6: Commit**

```
feat(settings): move shortcuts and privacy over, drop the old view

Completes the sidebar rebuild. The hotkey conflict logic moves across
unchanged, comments included: it's stateless on purpose.
```

---

## Task 9: `RichText` — o que ler do pasteboard

**Files:**
- Create: `MyPasteApp/Services/RichText.swift`
- Create: `MyPasteAppTests/RichTextReadTests.swift`

**Interfaces:**
- Produces:
  - `enum RichTextFormat: String { case rtf, html }` com
    `var pasteboardType: NSPasteboard.PasteboardType`
  - `static func RichText.preferredFormat(in types: [NSPasteboard.PasteboardType]) -> RichTextFormat?`

  A Task 10 (captura) e a Task 11 (escrita) consomem.

- [ ] **Step 1: Escrever os testes**

`MyPasteAppTests/RichTextReadTests.swift`:

```swift
//
//  RichTextReadTests.swift
//  MyPasteAppTests
//

import AppKit
import Testing

@testable import MyPasteApp

@Suite("Rich text — reading")
struct RichTextReadTests {
    @Test("Picks RTF when both are present")
    func prefersRTFOverHTML() {
        // Browsers offer both. RTF converts through NSAttributedString without
        // WebKit, while pasteboard HTML tends to carry layout markup and
        // external CSS that pastes back unpredictably.
        #expect(RichText.preferredFormat(in: [.html, .rtf, .string]) == .rtf)
    }

    @Test("Falls back to HTML when there is no RTF")
    func usesHTMLWhenAlone() {
        #expect(RichText.preferredFormat(in: [.html, .string]) == .html)
    }

    @Test("Plain text alone has no rich format")
    func plainTextHasNoRichFormat() {
        #expect(RichText.preferredFormat(in: [.string]) == nil)
    }

    @Test("An empty pasteboard has no rich format")
    func emptyHasNoRichFormat() {
        #expect(RichText.preferredFormat(in: []) == nil)
    }

    @Test("Unrelated types are ignored")
    func ignoresUnrelatedTypes() {
        #expect(RichText.preferredFormat(in: [.tiff, .fileURL]) == nil)
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:MyPasteAppTests/RichTextReadTests
```

Esperado: falha de compilação, `cannot find 'RichText' in scope`.

- [ ] **Step 3: Implementar a parte de leitura**

`MyPasteApp/Services/RichText.swift`:

```swift
//
//  RichText.swift
//  MyPasteApp
//
//  Decides which rich representation of a copied text to keep, and what to
//  hand back when pasting.
//
//  Both decisions are pure functions over types and stored data rather than
//  over an NSPasteboard, so they can be tested without touching the system
//  one — the same shape as `ClipboardMonitor.shouldCapture`.
//

import AppKit
import Foundation

enum RichTextFormat: String {
    case rtf
    case html

    var pasteboardType: NSPasteboard.PasteboardType {
        switch self {
        case .rtf:  return .rtf
        case .html: return .html
        }
    }
}

enum RichText {
    /// The best rich representation available among `types`, or nil when the
    /// pasteboard only carries plain text.
    ///
    /// RTF wins over HTML: `NSAttributedString` round-trips it without WebKit,
    /// whereas the HTML a browser puts on the pasteboard usually carries layout
    /// markup and external stylesheet references that paste back unpredictably.
    static func preferredFormat(in types: [NSPasteboard.PasteboardType]) -> RichTextFormat? {
        if types.contains(.rtf) { return .rtf }
        if types.contains(.html) { return .html }
        return nil
    }
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Mesmo comando do Step 2. Esperado: PASS, 5 testes.

- [ ] **Step 5: Commit**

```
feat(richtext): pick which rich format to keep from the pasteboard
```

---

## Task 10: O modelo e a captura de texto rico

**Files:**
- Modify: `MyPasteApp/Models/ClipboardItem.swift`
- Modify: `MyPasteApp/Services/ClipboardMonitor.swift:139-182`

**Interfaces:**
- Consumes: `RichText.preferredFormat(in:)` (Task 9).
- Produces: `ClipboardItem.richTextData: Data?`,
  `ClipboardItem.richTextTypeRaw: String?`, e o `var richTextFormat:
  RichTextFormat?` derivado. As Tasks 11 e 16 consomem.

- [ ] **Step 1: Acrescentar os campos ao modelo**

Em `ClipboardItem.swift`, depois de `textContent` (linha 23):

```swift
    /// The formatted representation of a text item, when the source app
    /// offered one. Kept out of the store like `imageData`, since RTF from a
    /// long document isn't small.
    @Attribute(.externalStorage) var richTextData: Data?
    /// Which format `richTextData` holds. See `RichTextFormat`.
    var richTextTypeRaw: String?
```

E a propriedade derivada, junto de `type` (linha 35):

```swift
    var richTextFormat: RichTextFormat? {
        get { richTextTypeRaw.flatMap(RichTextFormat.init(rawValue:)) }
        set { richTextTypeRaw = newValue?.rawValue }
    }
```

No `init`, acrescentar **dois parâmetros** depois de `textContent`, ambos com
padrão `nil`. O segundo recebe o enum, não a string crua, para que o chamador
não precise conhecer o `rawValue`:

```swift
        richTextData: Data? = nil,
        richTextFormat: RichTextFormat? = nil,
```

e as atribuições correspondentes no corpo:

```swift
        self.richTextData = richTextData
        self.richTextTypeRaw = richTextFormat?.rawValue
```

Os padrões mantêm todas as chamadas existentes compilando — inclusive as dos
testes de `RetentionPolicy`, que constroem `ClipboardItem` diretamente.

Campos opcionais permitem a migração leve do SwiftData, sem versionamento
manual de schema.

- [ ] **Step 2: Ler os tipos ricos na captura**

Em `ClipboardMonitor.readCurrentItem()`, o ramo de string (linhas 169-179) passa
a ser:

```swift
        if let str = pasteboard.string(forType: .string) {
            let trimmed = str.trimmingCharacters(in: .whitespacesAndNewlines)
            let isURL = URL(string: trimmed).map { $0.scheme != nil } ?? false
            let format = RichText.preferredFormat(in: pasteboard.types ?? [])
            return ClipboardItem(
                type: isURL ? .url : .text,
                // Both derived from the plain string, deliberately. Hashing the
                // RTF instead would break deduplication: the same text copied
                // from two apps carries different formatting bytes and would
                // come back as a new item every time. A preview with markup in
                // it would also be unreadable on the card.
                preview: String(str.prefix(Self.previewTextLength(from: defaults))),
                contentHash: Self.hash(str),
                textContent: str,
                richTextData: format.flatMap { pasteboard.data(forType: $0.pasteboardType) },
                richTextFormat: format,
                sourceAppBundleID: sourceApp
            )
        }
```

O parâmetro `richTextFormat:` do `init` recebe o enum e grava o `rawValue`.

- [ ] **Step 3: Rodar a suíte inteira**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS. Os testes de `RetentionPolicy` constroem `ClipboardItem`
diretamente e continuam válidos por causa dos valores padrão.

- [ ] **Step 4: Verificação manual da migração**

Abrir o app com o banco que já existe. Confirmar que o histórico anterior
aparece normalmente. Um schema que não migra se manifesta como store que não
abre — não como aviso.

- [ ] **Step 5: Commit**

```
feat(clipboard): keep the formatted version of copied text

contentHash and preview stay derived from the plain string: hashing the RTF
would make the same text from two apps look like two items.
```

---

## Task 11: `RichText` — o que escrever ao colar

**Files:**
- Modify: `MyPasteApp/Services/RichText.swift`
- Create: `MyPasteAppTests/RichTextWriteTests.swift`

**Interfaces:**
- Consumes: `RichTextFormat` (Task 9), os campos do modelo (Task 10).
- Produces: `static func RichText.payload(text:richTextData:format:plainOnly:) ->
  [(type: NSPasteboard.PasteboardType, data: Data)]`. A Task 12 consome.

Quem decide **o que** escrever é esta função pura; `ClipboardWriter` apenas
percorre o resultado. É o que torna verificável, sem um `NSPasteboard` real, a
invariante de que `.string` está sempre presente.

- [ ] **Step 1: Escrever os testes**

`MyPasteAppTests/RichTextWriteTests.swift`:

```swift
//
//  RichTextWriteTests.swift
//  MyPasteAppTests
//

import AppKit
import Testing

@testable import MyPasteApp

@Suite("Rich text — writing")
struct RichTextWriteTests {
    private let rtf = Data("{\\rtf1 hello}".utf8)

    private func payload(plainOnly: Bool,
                         richTextData: Data?,
                         format: RichTextFormat?) -> [(type: NSPasteboard.PasteboardType, data: Data)] {
        RichText.payload(text: "hello",
                         richTextData: richTextData,
                         format: format,
                         plainOnly: plainOnly)
    }

    @Test("Rich first, plain second, when formatting exists")
    func richComesFirst() {
        // Order is what tells the destination app which representation to
        // prefer.
        let result = payload(plainOnly: false, richTextData: rtf, format: .rtf)
        #expect(result.map(\.type) == [.rtf, .string])
    }

    @Test("Plain only when asked for plain")
    func plainOnlyDropsTheRichPart() {
        let result = payload(plainOnly: true, richTextData: rtf, format: .rtf)
        #expect(result.map(\.type) == [.string])
    }

    @Test("Plain only when the item has no formatting")
    func plainItemStaysPlain() {
        let result = payload(plainOnly: false, richTextData: nil, format: nil)
        #expect(result.map(\.type) == [.string])
    }

    @Test("Rich data without a format is ignored")
    func inconsistentItemDegradesToPlain() {
        // Defensive: a half-written row shouldn't produce a pasteboard entry
        // under an unknown type.
        let result = payload(plainOnly: false, richTextData: rtf, format: nil)
        #expect(result.map(\.type) == [.string])
    }

    @Test("Plain text is always present", arguments: [true, false])
    func plainIsAlwaysIncluded(plainOnly: Bool) {
        // A pasteboard carrying only RTF breaks pasting into any plain text
        // field — address bars, terminals, search boxes.
        let result = payload(plainOnly: plainOnly, richTextData: rtf, format: .rtf)
        #expect(result.contains { $0.type == .string })
    }

    @Test("The plain entry carries the text itself")
    func plainEntryHoldsTheText() {
        let result = payload(plainOnly: true, richTextData: nil, format: nil)
        #expect(result.first?.data == Data("hello".utf8))
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:MyPasteAppTests/RichTextWriteTests
```

Esperado: falha de compilação, `type 'RichText' has no member 'payload'`.

- [ ] **Step 3: Implementar**

Acrescentar a `RichText`:

```swift
    /// What to put on the pasteboard for a text item, in order of preference.
    ///
    /// Plain text is always included, even alongside a rich representation: a
    /// pasteboard carrying only RTF breaks pasting into any plain text field.
    /// The order matters — the first type declared is the one a destination
    /// app prefers.
    static func payload(text: String,
                        richTextData: Data?,
                        format: RichTextFormat?,
                        plainOnly: Bool) -> [(type: NSPasteboard.PasteboardType, data: Data)] {
        let plain = (type: NSPasteboard.PasteboardType.string, data: Data(text.utf8))
        guard !plainOnly, let richTextData, let format else { return [plain] }
        return [(type: format.pasteboardType, data: richTextData), plain]
    }
```

- [ ] **Step 4: Rodar e confirmar que passa**

Mesmo comando do Step 2. Esperado: PASS, 7 testes.

- [ ] **Step 5: Commit**

```
feat(richtext): decide what goes on the pasteboard when pasting

Plain text is always included: a pasteboard carrying only RTF breaks
pasting into address bars, terminals and search fields.
```

---

## Task 12: `ClipboardWriter` e o modificador `⇧`

**Files:**
- Modify: `MyPasteApp/Services/ClipboardWriter.swift`
- Modify: `MyPasteApp/Views/OverlayView.swift`

**Interfaces:**
- Consumes: `RichText.payload(...)` (Task 11), `PreferenceKeys.alwaysPastePlainText`
  (Task 1).
- Produces: `ClipboardWriter.write(_ item: ClipboardItem, plainText: Bool = false)`.
  A Task 14 (`ItemActions`) consome.

- [ ] **Step 1: Escrever pela lista de pares**

Em `ClipboardWriter.swift`, substituir `write` inteira:

```swift
    /// Writes the item to the pasteboard.
    ///
    /// `plainText` drops any formatting for this paste only; the item keeps
    /// what it captured. The caller resolves it from the preference and the ⇧
    /// modifier together.
    func write(_ item: ClipboardItem, plainText: Bool = false) {
        let pb = NSPasteboard.general
        monitor?.ignoreNextChange = true
        pb.clearContents()

        item.createdAt = .now
        item.lastUsedAt = .now
        try? item.modelContext?.save()

        switch item.type {
        case .text, .url:
            guard let text = item.textContent else { break }
            for entry in RichText.payload(text: text,
                                          richTextData: item.richTextData,
                                          format: item.richTextFormat,
                                          plainOnly: plainText) {
                pb.setData(entry.data, forType: entry.type)
            }
        case .image:
            if let data = item.imageData, let img = NSImage(data: data) {
                pb.writeObjects([img])
            }
        case .file:
            if let strings = item.fileURLStrings {
                let urls = strings.map { URL(fileURLWithPath: $0) as NSURL }
                pb.writeObjects(urls)
            }
        }
    }
```

- [ ] **Step 2: Resolver o modo no ponto da colagem**

Em `OverlayView.swift`, acrescentar a preferência e um resolvedor:

```swift
    @AppStorage(PreferenceKeys.alwaysPastePlainText) private var alwaysPastePlainText = false

    /// Whether this paste should hand over plain text.
    ///
    /// `onTapGesture` doesn't report modifiers, so ⇧ is read from the current
    /// event at the moment of the click. With the preference on, ⇧ changes
    /// nothing: it always means "plain", never "the opposite of my default".
    private var pastesPlainText: Bool {
        alwaysPastePlainText
            || NSEvent.modifierFlags.contains(.shift)
    }
```

`pick(_:)` passa a repassar esse valor, e `onPick` ganha o segundo argumento:

```swift
    private func pick(_ item: ClipboardItem, plainText: Bool? = nil) {
        item.lastUsedAt = .now
        try? modelContext.save()
        onPick(item, plainText ?? pastesPlainText)
    }
```

A assinatura de `onPick` vira `(ClipboardItem, Bool) -> Void`; ajustar o `init`
e o chamador em `OverlayWindowController.prepare()` (linha 68), que repassa o
booleano ao `ClipboardWriter` do `AppDelegate`.

- [ ] **Step 3: Acrescentar `⇧↵` explicitamente**

O `onKeyPress(.return)` atual não distingue o modificador. Substituir por:

```swift
        .onKeyPress(.return) { press in
            guard let item = filtered.first(where: { $0.id == selectedID }) else {
                return .ignored
            }
            pick(item, plainText: alwaysPastePlainText || press.modifiers.contains(.shift))
            return .handled
        }
```

- [ ] **Step 4: Compilar e rodar a suíte**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS.

- [ ] **Step 5: Verificação manual**

Copiar um trecho formatado do Notas ou de uma página web. Colar num documento
formatado e confirmar que a formatação chegou. Colar de novo com `⇧↵` e
confirmar que chegou limpo. Ligar "Always paste as plain text" em Settings e
confirmar que as duas colagens passam a chegar limpas.

- [ ] **Step 6: Commit**

```
feat(paste): paste formatted text, with ⇧ for plain

Item 7 of the roadmap: the app stops flattening what it captures. The
preference forces plain for every paste; ⇧ does it for one.
```

---

## Task 13: `ItemActions`

**Files:**
- Create: `MyPasteApp/Services/ItemActions.swift`
- Modify: `MyPasteApp/Views/OverlayView.swift`

**Interfaces:**
- Consumes: `ClipboardWriter.write(_:plainText:)` (Task 12).
- Produces: `@MainActor final class ItemActions` com `paste(_:plainText:)`,
  `copy(_:)`, `togglePin(_:)`, `delete(_:)`. As Tasks 14, 17, 19 e 22
  acrescentam métodos e a Task 14 o consome pelo menu.

As ações saem da view para que o menu de contexto e os atalhos de teclado sejam
dois chamadores do mesmo conjunto — é o que impede que as duas entradas
divirjam.

- [ ] **Step 1: Criar o tipo**

`MyPasteApp/Services/ItemActions.swift`:

```swift
//
//  ItemActions.swift
//  MyPasteApp
//

import AppKit
import SwiftData

/// Everything that can be done to a single history item, with no interface
/// attached.
///
/// The context menu and the keyboard shortcuts are two callers of this same
/// set. Keeping the behaviour here is what stops the menu entry and the
/// shortcut from drifting apart as the roadmap adds more of both.
@MainActor
final class ItemActions {
    private let modelContext: ModelContext
    private let writer: ClipboardWriter
    private let onPaste: (ClipboardItem, Bool) -> Void

    init(modelContext: ModelContext,
         writer: ClipboardWriter,
         onPaste: @escaping (ClipboardItem, Bool) -> Void) {
        self.modelContext = modelContext
        self.writer = writer
        self.onPaste = onPaste
    }

    func paste(_ item: ClipboardItem, plainText: Bool) {
        item.lastUsedAt = .now
        try? modelContext.save()
        onPaste(item, plainText)
    }

    /// Puts the item on the pasteboard without simulating ⌘V.
    ///
    /// Goes through `ClipboardWriter` like pasting does, so it inherits both
    /// the `ignoreNextChange` that keeps the monitor from recapturing our own
    /// write, and the promotion to the top of the history. Copying counts as
    /// use, the same way pasting does.
    func copy(_ item: ClipboardItem, plainText: Bool = false) {
        writer.write(item, plainText: plainText)
    }

    func togglePin(_ item: ClipboardItem) {
        item.isPinned.toggle()
        try? modelContext.save()
    }

    func delete(_ item: ClipboardItem) {
        modelContext.delete(item)
        try? modelContext.save()
    }
}
```

- [ ] **Step 2: Fazer a `OverlayView` usar o tipo**

Construir uma instância em `OverlayView` e redirecionar `pick`, o `⌘P` do
`onKeyPress(keys: ["p"])` e o `delete(_:)` para ela. **Preservar** a lógica de
reposicionamento da seleção que `OverlayView.delete` faz hoje (linhas 131-146):
ela é da view, não da ação — decide qual card fica selecionado depois da
remoção, e `ItemActions.delete` não tem como saber disso.

- [ ] **Step 3: Compilar e rodar a suíte**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS.

- [ ] **Step 4: Commit**

```
refactor(overlay): move item actions out of the view

The context menu and the keyboard shortcuts become two callers of one set,
so a menu entry and its shortcut can't drift apart.
```

---

## Task 14: O menu de contexto do card

**Files:**
- Create: `MyPasteApp/Views/ItemContextMenu.swift`
- Modify: `MyPasteApp/Views/OverlayView.swift`

**Interfaces:**
- Consumes: `ItemActions` (Task 13).

O menu nasce com as ações que já existem. `⌘E`, `⌘R` e `␣` entram nas Tasks 17,
18 e 22 — um menu com entradas mortas seria pior que um menu curto.

- [ ] **Step 1: Criar o menu**

`MyPasteApp/Views/ItemContextMenu.swift`:

```swift
//
//  ItemContextMenu.swift
//  MyPasteApp
//

import SwiftUI

/// The right-click menu of a card.
///
/// Presentation only — the behaviour lives in `ItemActions`. The shortcut shown
/// next to each entry is how the user discovers it; an entry whose shortcut
/// doesn't actually work is worse than no entry at all, so nothing is listed
/// here before it works.
///
/// `⌘O Open` belongs above the paste group and arrives with roadmap item 21.
struct ItemContextMenu: View {
    let item: ClipboardItem
    let actions: ItemActions
    /// Name of the app the paste would land in, when it's known.
    let destinationAppName: String?

    var body: some View {
        Button(pasteTitle) { actions.paste(item, plainText: false) }
        if item.type == .text || item.type == .url {
            Button("Paste as Plain Text") { actions.paste(item, plainText: true) }
        }
        Button("Copy") { actions.copy(item) }

        Divider()

        Button("Delete") { actions.delete(item) }

        Divider()

        Button(item.isPinned ? "Unpin" : "Pin") { actions.togglePin(item) }
    }

    /// Naming the destination says exactly what will happen; the app is already
    /// known by the time the drawer opens.
    private var pasteTitle: String {
        guard let destinationAppName else { return "Paste" }
        return "Paste in \(destinationAppName)"
    }
}
```

SwiftUI monta os atalhos do menu a partir do `.keyboardShortcut` quando
aplicável; aqui os atalhos continuam sendo tratados por `onKeyPress` na
`OverlayView`, e o menu é a superfície de descoberta.

- [ ] **Step 2: Ligar ao card**

Em `OverlayView`, no `ForEach`, acrescentar depois do `.onTapGesture`:

```swift
                            .contextMenu {
                                ItemContextMenu(item: item,
                                                actions: actions,
                                                destinationAppName: destinationAppName)
                            }
```

`destinationAppName` vem do `previousApp` que `OverlayWindowController` já
guarda (linha 119) — expor via a `init` da `OverlayView`, como já é feito com
`onPick` e `onDismiss`.

- [ ] **Step 3: Compilar e rodar a suíte**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS.

- [ ] **Step 4: Verificação manual**

Clicar com o botão direito num card. Confirmar que o menu abre, que "Paste in
<App>" nomeia o app correto, que "Paste as Plain Text" não aparece em itens de
imagem e arquivo, e que cada entrada faz o que diz.

- [ ] **Step 5: Commit**

```
feat(overlay): add a context menu to the card

The chassis the rest of the phase hangs actions on. Entries arrive as their
features do — a menu advertising a dead shortcut is worse than a short menu.
```

---

## Task 15: `RichTextEditor`

**Files:**
- Create: `MyPasteApp/Views/RichTextEditor.swift`

**Interfaces:**
- Produces: `struct RichTextEditor: NSViewRepresentable` com
  `@Binding var attributedText: NSAttributedString` e `var isRichText: Bool`.
  A Task 16 consome.

`NSTextView` e não `TextEditor` do SwiftUI: é o que edita texto rico por padrão
e o que traz `⌘B`, `⌘I`, o menu Formatar e — no macOS 15+ — as Writing Tools,
tudo sem código nosso. Isso é o que torna o item 22 quase gratuito mais adiante.

- [ ] **Step 1: Implementar**

`MyPasteApp/Views/RichTextEditor.swift`:

```swift
//
//  RichTextEditor.swift
//  MyPasteApp
//

import AppKit
import SwiftUI

/// An `NSTextView` wrapped for SwiftUI, editing rich text.
///
/// Not `TextEditor`: that one is plain-text only, and the whole point here is
/// to stop destroying the formatting the app now captures. `NSTextView` also
/// brings ⌘B, ⌘I, the Format menu and macOS 15+ Writing Tools for free.
struct RichTextEditor: NSViewRepresentable {
    @Binding var attributedText: NSAttributedString
    var isRichText: Bool = true

    func makeNSView(context: Context) -> NSScrollView {
        let scrollView = NSTextView.scrollableTextView()
        guard let textView = scrollView.documentView as? NSTextView else {
            return scrollView
        }
        textView.delegate = context.coordinator
        textView.isRichText = isRichText
        textView.allowsUndo = true
        textView.isEditable = true
        textView.isSelectable = true
        textView.autoresizingMask = [.width]
        textView.textContainerInset = NSSize(width: 8, height: 8)
        if !isRichText {
            textView.font = .monospacedSystemFont(ofSize: 13, weight: .regular)
        }
        textView.textStorage?.setAttributedString(attributedText)
        return scrollView
    }

    func updateNSView(_ scrollView: NSScrollView, context: Context) {
        guard let textView = scrollView.documentView as? NSTextView else { return }
        // Only push back when the model actually diverged. Rewriting the
        // storage on every SwiftUI update would reset the insertion point to
        // the start of the document on each keystroke.
        guard textView.textStorage?.isEqual(to: attributedText) == false else { return }
        textView.textStorage?.setAttributedString(attributedText)
    }

    func makeCoordinator() -> Coordinator {
        Coordinator(text: $attributedText)
    }

    final class Coordinator: NSObject, NSTextViewDelegate {
        private let text: Binding<NSAttributedString>

        init(text: Binding<NSAttributedString>) {
            self.text = text
        }

        func textDidChange(_ notification: Notification) {
            guard let textView = notification.object as? NSTextView,
                  let storage = textView.textStorage else { return }
            text.wrappedValue = NSAttributedString(attributedString: storage)
        }
    }
}
```

- [ ] **Step 2: Compilar**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild build \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: BUILD SUCCEEDED. Sem teste: é envelope de AppKit, verificado na Task
16 pela GUI.

- [ ] **Step 3: Commit**

```
feat(editor): wrap NSTextView for SwiftUI

NSTextView rather than TextEditor: it edits rich text, which is the point,
and brings ⌘B/⌘I and Writing Tools with it.
```

---

## Task 16: A janela do editor

**Files:**
- Create: `MyPasteApp/Views/ItemEditorView.swift`
- Create: `MyPasteApp/Window/ItemEditorWindowController.swift`
- Create: `MyPasteAppTests/ItemEditTests.swift`
- Modify: `MyPasteApp/Services/ItemActions.swift`
- Modify: `MyPasteApp/Views/ItemContextMenu.swift`
- Modify: `MyPasteApp/Views/OverlayView.swift`

**Interfaces:**
- Consumes: `RichTextEditor` (Task 15), `ItemActions` (Task 13).
- Produces: `ItemEdit.apply(to:attributed:label:previewLength:)` — a função pura
  que recalcula os campos derivados. A Task 19 consome.
  `ItemEditorWindowController.open(item:focus:)` com
  `enum ItemEditorFocus { case body, label }`.

- [ ] **Step 1: Escrever os testes do recálculo**

`MyPasteAppTests/ItemEditTests.swift`:

```swift
//
//  ItemEditTests.swift
//  MyPasteAppTests
//

import Foundation
import SwiftData
import Testing

@testable import MyPasteApp

@MainActor
@Suite("Item editing")
final class ItemEditTests {
    private let container: ModelContainer

    init() throws {
        container = try ModelContainer(
            for: ClipboardItem.self,
            configurations: ModelConfiguration(isStoredInMemoryOnly: true)
        )
    }

    private func makeItem(text: String) -> ClipboardItem {
        let item = ClipboardItem(
            type: .text,
            preview: text,
            contentHash: ClipboardMonitor.hash(text),
            textContent: text
        )
        container.mainContext.insert(item)
        return item
    }

    @Test("Saving rewrites the plain text")
    func rewritesPlainText() {
        let item = makeItem(text: "before")
        ItemEdit.apply(to: item,
                       attributed: NSAttributedString(string: "after"),
                       label: nil,
                       previewLength: 200)
        #expect(item.textContent == "after")
    }

    @Test("The hash is recomputed from the plain text")
    func recomputesHash() {
        // Without this the edited item collides with the original in
        // deduplication and disappears on the next identical copy.
        let item = makeItem(text: "before")
        ItemEdit.apply(to: item,
                       attributed: NSAttributedString(string: "after"),
                       label: nil,
                       previewLength: 200)
        #expect(item.contentHash == ClipboardMonitor.hash("after"))
    }

    @Test("The preview honours the configured length")
    func previewRespectsLength() {
        // Not a hardcoded 200: the length is a preference, and a card showing
        // the old text while the paste hands over the new one is hard to
        // diagnose later.
        let item = makeItem(text: "before")
        let long = String(repeating: "x", count: 500)
        ItemEdit.apply(to: item,
                       attributed: NSAttributedString(string: long),
                       label: nil,
                       previewLength: 80)
        #expect(item.preview.count == 80)
    }

    @Test("Editing promotes the item to the top")
    func promotesToTop() {
        let item = makeItem(text: "before")
        item.createdAt = .distantPast
        ItemEdit.apply(to: item,
                       attributed: NSAttributedString(string: "after"),
                       label: nil,
                       previewLength: 200)
        #expect(item.createdAt.timeIntervalSinceNow > -5)
    }

    @Test("An empty label is stored as no label")
    func emptyLabelBecomesNil() {
        let item = makeItem(text: "text")
        ItemEdit.apply(to: item,
                       attributed: NSAttributedString(string: "text"),
                       label: "   ",
                       previewLength: 200)
        #expect(item.label == nil)
    }

    @Test("Saving keeps the formatting as RTF")
    func storesRichText() {
        let item = makeItem(text: "before")
        let attributed = NSAttributedString(
            string: "after",
            attributes: [.font: NSFont.boldSystemFont(ofSize: 13)]
        )
        ItemEdit.apply(to: item,
                       attributed: attributed,
                       label: nil,
                       previewLength: 200)
        #expect(item.richTextData != nil)
        #expect(item.richTextFormat == .rtf)
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:MyPasteAppTests/ItemEditTests
```

Esperado: falha de compilação, `cannot find 'ItemEdit' in scope`.

- [ ] **Step 3: Implementar o recálculo**

Acrescentar a `Services/ItemActions.swift`:

```swift
/// Applies an edit to an item, recomputing everything derived from its text.
///
/// Pure and free-standing so the recalculation is testable without a window:
/// each of these five fields, left stale, produces a different bug.
enum ItemEdit {
    static func apply(to item: ClipboardItem,
                      attributed: NSAttributedString,
                      label: String?,
                      previewLength: Int) {
        let plain = attributed.string

        item.textContent = plain
        // Derived from the plain text, never from the RTF — see the comment in
        // ClipboardMonitor.readCurrentItem.
        item.preview = String(plain.prefix(previewLength))
        item.contentHash = ClipboardMonitor.hash(plain)

        let range = NSRange(location: 0, length: attributed.length)
        item.richTextData = try? attributed.data(
            from: range,
            documentAttributes: [.documentType: NSAttributedString.DocumentType.rtf]
        )
        item.richTextFormat = item.richTextData == nil ? nil : .rtf

        let trimmed = label?.trimmingCharacters(in: .whitespacesAndNewlines)
        item.label = (trimmed?.isEmpty ?? true) ? nil : trimmed

        // Editing counts as use, the same way pasting does. `createdAt` is
        // already "last used" rather than "captured at" — see the note at the
        // top of ROADMAP.md.
        item.createdAt = .now
        item.lastUsedAt = .now

        try? item.modelContext?.save()
    }
}
```

`item.label` só existe a partir da Task 17. Para manter esta tarefa compilando
por si, **acrescentar o campo `var label: String?` ao modelo agora**, junto do
`init` com padrão `nil`; a Task 17 cuida do card e da busca.

- [ ] **Step 4: Rodar e confirmar que passa**

Mesmo comando do Step 2. Esperado: PASS, 6 testes.

- [ ] **Step 5: Criar a view do editor**

`MyPasteApp/Views/ItemEditorView.swift`:

```swift
//
//  ItemEditorView.swift
//  MyPasteApp
//

import AppKit
import SwiftUI

/// Which field the editor starts focused on.
///
/// One window serves ⌘E, ⌘R and ⌘N; only the initial focus differs. A separate
/// rename screen would duplicate the same form to move one focus ring.
enum ItemEditorFocus {
    case body
    case label
}

struct ItemEditorView: View {
    let item: ClipboardItem
    let initialFocus: ItemEditorFocus
    let onClose: () -> Void

    @AppStorage(PreferenceKeys.previewTextLength) private var previewTextLength: Int = 200
    @State private var attributed: NSAttributedString
    @State private var label: String
    @FocusState private var labelFocused: Bool

    init(item: ClipboardItem,
         initialFocus: ItemEditorFocus,
         onClose: @escaping () -> Void) {
        self.item = item
        self.initialFocus = initialFocus
        self.onClose = onClose
        // Prefer the stored formatting; fall back to the plain text for an
        // item captured before rich text existed, or written by hand.
        let initial: NSAttributedString
        if let data = item.richTextData,
           let restored = NSAttributedString(rtf: data, documentAttributes: nil) {
            initial = restored
        } else {
            initial = NSAttributedString(string: item.textContent ?? "")
        }
        _attributed = State(initialValue: initial)
        _label = State(initialValue: item.label ?? "")
    }

    var body: some View {
        VStack(spacing: 0) {
            TextField("Label", text: $label)
                .textFieldStyle(.roundedBorder)
                .focused($labelFocused)
                .padding(12)

            Divider()

            RichTextEditor(attributedText: $attributed)
                .frame(minWidth: 480, minHeight: 280)

            Divider()

            HStack {
                Spacer()
                Button("Cancel", role: .cancel) { onClose() }
                    .keyboardShortcut(.cancelAction)
                Button("Save") { save() }
                    .keyboardShortcut(.defaultAction)
            }
            .padding(12)
        }
        .onAppear { labelFocused = (initialFocus == .label) }
    }

    private func save() {
        ItemEdit.apply(to: item,
                       attributed: attributed,
                       label: label,
                       previewLength: previewTextLength)
        onClose()
    }
}
```

O foco do corpo é o padrão do `NSTextView` quando `labelFocused` é falso, então
`ItemEditorFocus.body` não precisa de tratamento próprio.

- [ ] **Step 6: Criar o controlador da janela**

`MyPasteApp/Window/ItemEditorWindowController.swift`:

```swift
//
//  ItemEditorWindowController.swift
//  MyPasteApp
//

import AppKit
import SwiftData
import SwiftUI

/// The editor window.
///
/// A real titled window rather than something inside the overlay: the overlay
/// is `.transient` and hides on click-outside and on losing focus, none of
/// which survives an editing session.
@MainActor
final class ItemEditorWindowController: NSObject {
    private var window: NSWindow?
    private let modelContainer: ModelContainer

    init(modelContainer: ModelContainer) {
        self.modelContainer = modelContainer
        super.init()
    }

    func open(item: ClipboardItem, focus: ItemEditorFocus) {
        close()

        let root = ItemEditorView(item: item, initialFocus: focus) { [weak self] in
            self?.close()
        }
        .modelContainer(modelContainer)

        let window = NSWindow(
            contentRect: NSRect(x: 0, y: 0, width: 520, height: 400),
            styleMask: [.titled, .closable, .resizable],
            backing: .buffered,
            defer: false
        )
        window.title = item.label ?? "Edit Item"
        window.contentView = NSHostingView(rootView: root)
        window.center()
        window.isReleasedWhenClosed = false
        // The editor shows the item's contents, which is exactly the data the
        // screen-sharing preference exists to protect.
        window.sharingType = WindowPrivacy.sharingType()

        NSApp.activate(ignoringOtherApps: true)
        window.makeKeyAndOrderFront(nil)
        self.window = window
    }

    func close() {
        window?.orderOut(nil)
        window = nil
    }
}
```

Instanciar o controlador no `AppDelegate`, ao lado do de Preferências, e passar
a referência para `ItemActions`.

- [ ] **Step 7: Ligar `⌘E` e a entrada de menu**

Acrescentar `edit(_:)` a `ItemActions`, chamando o controlador. Em
`OverlayView`, `⌘E` sobre o item selecionado abre o editor e fecha a overlay.
Acrescentar a entrada "Edit" ao `ItemContextMenu`, **apenas para itens de texto
e URL** — imagem e arquivo não são editáveis por texto (o item 20 dá a imagens
outra forma de edição, na Fase 6).

- [ ] **Step 8: Rodar a suíte inteira**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS.

- [ ] **Step 9: Verificação manual**

`⌘E` num card de texto: a janela abre com o foco no corpo, o texto está lá,
`⌘Z` desfaz, `⌘B` aplica negrito. Salvar: o card mostra o texto novo e colar
entrega o texto novo com a formatação. Cancelar: nada muda. Confirmar que `⌘E`
não faz nada num item de imagem.

- [ ] **Step 10: Commit**

```
feat(editor): edit an item without leaving the app

Roadmap item 8. Saving recomputes hash, preview and rich data together:
each of them left stale is a different bug.
```

---

## Task 17: Rótulos

**Files:**
- Modify: `MyPasteApp/Views/ClipboardCardView.swift:73-114`
- Modify: `MyPasteApp/Views/OverlayView.swift:28-39`
- Modify: `MyPasteApp/Views/ItemContextMenu.swift`
- Modify: `MyPasteApp/Services/ItemActions.swift`
- Create: `MyPasteAppTests/ItemSearchTests.swift`

**Interfaces:**
- Consumes: `ClipboardItem.label` (acrescentado na Task 16),
  `ItemEditorWindowController.open(item:focus:)` (Task 16).
- Produces: `OverlayView.matches(item:query:)` como função estática pura, para
  que o filtro seja testável.

- [ ] **Step 1: Escrever os testes de busca**

`MyPasteAppTests/ItemSearchTests.swift`:

```swift
//
//  ItemSearchTests.swift
//  MyPasteAppTests
//

import Foundation
import SwiftData
import Testing

@testable import MyPasteApp

@MainActor
@Suite("Item search")
final class ItemSearchTests {
    private let container: ModelContainer

    init() throws {
        container = try ModelContainer(
            for: ClipboardItem.self,
            configurations: ModelConfiguration(isStoredInMemoryOnly: true)
        )
    }

    private func item(preview: String, text: String?, label: String?) -> ClipboardItem {
        let item = ClipboardItem(
            type: .text,
            preview: preview,
            contentHash: preview,
            textContent: text
        )
        item.label = label
        container.mainContext.insert(item)
        return item
    }

    @Test("Finds by content")
    func findsByContent() {
        let subject = item(preview: "hello world", text: "hello world", label: nil)
        #expect(OverlayView.matches(item: subject, query: "world"))
    }

    @Test("Finds by label")
    func findsByLabel() {
        // A label that isn't searchable defeats the point of naming an item.
        let subject = item(preview: "xyzzy", text: "xyzzy", label: "Deploy key")
        #expect(OverlayView.matches(item: subject, query: "deploy"))
    }

    @Test("Matching ignores case")
    func caseInsensitive() {
        let subject = item(preview: "xyzzy", text: "xyzzy", label: "Deploy Key")
        #expect(OverlayView.matches(item: subject, query: "DEPLOY"))
    }

    @Test("An unrelated query matches nothing")
    func noFalsePositives() {
        let subject = item(preview: "hello", text: "hello", label: "Greeting")
        #expect(!OverlayView.matches(item: subject, query: "goodbye"))
    }

    @Test("An item with no label is still searchable by content")
    func labelIsOptional() {
        let subject = item(preview: "hello", text: "hello", label: nil)
        #expect(OverlayView.matches(item: subject, query: "hell"))
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:MyPasteAppTests/ItemSearchTests
```

Esperado: falha de compilação, `type 'OverlayView' has no member 'matches'`.

- [ ] **Step 3: Extrair o filtro e incluir o rótulo**

Em `OverlayView`, acrescentar:

```swift
    /// Whether an item satisfies the search query.
    ///
    /// Static and pure so the rule is testable without rendering the overlay.
    static func matches(item: ClipboardItem, query: String) -> Bool {
        let q = query.lowercased()
        guard !q.isEmpty else { return true }
        if item.preview.lowercased().contains(q) { return true }
        if item.textContent?.lowercased().contains(q) == true { return true }
        if item.label?.lowercased().contains(q) == true { return true }
        return false
    }
```

E `filtered` passa a usá-la:

```swift
        return sorted.filter { Self.matches(item: $0, query: searchText) }
```

- [ ] **Step 4: Mostrar o rótulo no cabeçalho**

Em `ClipboardCardView.coloredHeader`, quando `item.label` existir ele substitui
`typeLabel` e `relativeTime`:

```swift
            VStack(alignment: .leading, spacing: 2) {
                if let label = item.label {
                    // Takes over the space of type + relative time rather than
                    // competing with it — see design-refs/13-pinboard-links-uteis.png.
                    Text(label)
                        .font(.system(size: 15, weight: .semibold))
                        .foregroundStyle(.white)
                        .lineLimit(1)
                        .truncationMode(.tail)
                } else {
                    Text(typeLabel)
                        .font(.system(size: 15, weight: .semibold))
                        .foregroundStyle(.white)
                    Text(relativeTime)
                        .font(.system(size: 11))
                        .foregroundStyle(.white.opacity(0.85))
                }
            }
```

- [ ] **Step 5: Ligar `⌘R` e a entrada de menu**

Acrescentar `rename(_:)` a `ItemActions`, abrindo o mesmo editor com
`focus: .label`. Ligar `⌘R` em `OverlayView` e acrescentar "Rename" ao
`ItemContextMenu`, no mesmo grupo de "Edit".

Diferente de "Edit", "Rename" vale para **todos** os tipos: uma imagem ou um
arquivo também se beneficiam de um nome.

- [ ] **Step 6: Rodar a suíte inteira**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS.

- [ ] **Step 7: Verificação manual**

`⌘R` num card: a janela abre com o foco no campo de rótulo. Nomear, salvar, e
confirmar que o cabeçalho passa a mostrar o nome. Buscar pelo nome e achar o
item. Apagar o rótulo e confirmar que o cabeçalho volta a tipo + tempo. Rotular
com um nome longo e confirmar que o layout não quebra.

- [ ] **Step 8: Commit**

```
feat(items): name an item and find it by that name

Roadmap item 9. The label takes over the header space of type + relative
time, and joins the search — a label nobody can search for is decoration.
```

---

## Task 18: Criar um item do zero

**Files:**
- Modify: `MyPasteApp/Services/ItemActions.swift`
- Modify: `MyPasteApp/Views/OverlayView.swift`
- Modify: `MyPasteApp/AppDelegate.swift`
- Create: `MyPasteAppTests/ManualItemTests.swift`

**Interfaces:**
- Consumes: `ItemEditorWindowController` (Task 16).
- Produces: `static func ItemActions.makeManualItem(text:) -> ClipboardItem`.

**Desvio da spec, deliberado.** A spec diz que o item criado fica com
`sourceAppBundleID = nil` e que `AppColorExtractor` e `ClipboardCardView` ganham
um caso para nulo. Gravar `Bundle.main.bundleIdentifier` entrega o mesmo
resultado visual **sem código novo em nenhum dos dois**: a cor sai do ícone do
MyPasteApp pelo caminho que já existe, e o ícone idem. É também semanticamente
verdadeiro — a origem do item é este app — e faz o filtro por app do item 12
listá-lo corretamente em vez de precisar de um grupo "Desconhecido".

- [ ] **Step 1: Escrever os testes**

`MyPasteAppTests/ManualItemTests.swift`:

```swift
//
//  ManualItemTests.swift
//  MyPasteAppTests
//

import Foundation
import Testing

@testable import MyPasteApp

@MainActor
@Suite("Manually created items")
struct ManualItemTests {
    @Test("Plain text becomes a text item")
    func detectsText() {
        #expect(ItemActions.makeManualItem(text: "just a note").type == .text)
    }

    @Test("A URL becomes a url item")
    func detectsURL() {
        // Same rule the monitor uses when capturing.
        #expect(ItemActions.makeManualItem(text: "https://example.com").type == .url)
    }

    @Test("The item is pinned on creation")
    func startsPinned() {
        // isPinned is the only thing RetentionPolicy protects from pruning
        // today, in both passes. Without it a hand-written snippet unused for
        // 30 days is deleted — the one case in this phase where the app would
        // destroy authored work. Roadmap item 17 replaces this with expiresAt.
        #expect(ItemActions.makeManualItem(text: "note").isPinned)
    }

    @Test("The source app is this app")
    func sourceIsThisApp() {
        // Not nil: this is what gives the card its colour and icon through the
        // path that already exists, and it happens to be true.
        #expect(ItemActions.makeManualItem(text: "note").sourceAppBundleID
                == Bundle.main.bundleIdentifier)
    }

    @Test("The hash matches the text")
    func hashesTheText() {
        #expect(ItemActions.makeManualItem(text: "note").contentHash
                == ClipboardMonitor.hash("note"))
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:MyPasteAppTests/ManualItemTests
```

Esperado: falha de compilação, `type 'ItemActions' has no member 'makeManualItem'`.

- [ ] **Step 3: Implementar**

Acrescentar a `ItemActions`:

```swift
    /// Builds an item the user wrote by hand.
    ///
    /// Pinned on creation because `isPinned` is the only thing
    /// `RetentionPolicy.prune()` protects today, in both passes. Roadmap item
    /// 17 (`expiresAt`) is the proper answer and replaces this when it exists.
    static func makeManualItem(text: String) -> ClipboardItem {
        let trimmed = text.trimmingCharacters(in: .whitespacesAndNewlines)
        let isURL = URL(string: trimmed).map { $0.scheme != nil } ?? false
        return ClipboardItem(
            type: isURL ? .url : .text,
            preview: String(text.prefix(ClipboardMonitor.previewTextLength())),
            contentHash: ClipboardMonitor.hash(text),
            textContent: text,
            // This app really is where the item came from, which is also what
            // gives the card its colour and icon with no extra code.
            sourceAppBundleID: Bundle.main.bundleIdentifier,
            isPinned: true
        )
    }
```

- [ ] **Step 4: Rodar e confirmar que passa**

Mesmo comando do Step 2. Esperado: PASS, 5 testes.

- [ ] **Step 5: Ligar `⌘N`**

Em `OverlayView`, `⌘N` abre o editor vazio e fecha a overlay. Ao salvar um
editor vazio, criar o item via `makeManualItem(text:)`, inseri-lo no contexto e
então aplicar `ItemEdit.apply(...)` para gravar rótulo e formatação.

Acrescentar ao menu da barra de status, em `AppDelegate.showStatusMenu()`, o
item "New Text Item ⌘N" acima de "Preferences…", seguindo
`design-refs/05-menu-status-bar.png`.

- [ ] **Step 6: Rodar a suíte inteira**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS.

- [ ] **Step 7: Verificação manual**

`⌘N` na overlay e pelo menu da barra de status. Digitar, salvar, e confirmar: o
item aparece no histórico, tem cabeçalho colorido e ícone (não fica cinza sem
ícone), está fixado, e colar entrega o texto. Colar uma URL e confirmar que
nasce como item de URL.

- [ ] **Step 8: Commit**

```
feat(items): create an item from scratch

Roadmap item 10. Pinned on creation: isPinned is the only thing prune()
spares today, and a hand-written snippet shouldn't expire like a copy.
```

---

## Task 19: Preview — spike de viabilidade

**Files:**
- Create: `MyPasteApp/Window/ItemPreviewPanel.swift` (descartável)
- Modify: `MyPasteApp/Window/OverlayWindowController.swift:229-242`

**Este passo decide se o item 19 fica na fase.** Se o painel não se sustentar em
**duas tentativas**, o item sai, volta ao roadmap com o resultado registrado, e
a fase segue para a verificação final. As outras entregas não dependem dele.

- [ ] **Step 1: Montar o painel mínimo**

Um `NSPanel` `.nonactivatingPanel` posicionado acima da overlay, exibindo um
`Text` fixo. Sem conteúdo real, sem chrome — só a janela. O objetivo aqui é
exclusivamente descobrir como as duas janelas convivem.

```swift
        let panel = NSPanel(
            contentRect: NSRect(x: 0, y: 0, width: 520, height: 380),
            // Not .borderless: unlike the overlay, this panel doesn't need to
            // become key — the overlay keeps the keyboard. See Step 3.
            styleMask: [.nonactivatingPanel, .titled, .closable],
            backing: .buffered,
            defer: false
        )
        panel.isFloatingPanel = true
        // Above the overlay, which is already .floating.
        panel.level = .floating
        panel.collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary, .transient]
        panel.hidesOnDeactivate = false
        panel.sharingType = WindowPrivacy.sharingType()
        panel.contentView = NSHostingView(rootView: Text("preview spike"))
```

`OverlayWindowController` guarda a referência numa propriedade
`private var previewPanel: NSPanel?`, que o Step 2 consulta.

- [ ] **Step 2: Ensinar os monitores a ignorá-lo**

`installClickOutsideMonitors()` esconde a overlay a qualquer clique cuja
`event.window` não seja ela. O painel de preview é uma segunda janela nossa e
precisa entrar na mesma exceção:

```swift
        localMouseMonitor = NSEvent.addLocalMonitorForEvents(matching: mask) { [weak self] event in
            guard let self else { return event }
            // The preview panel is ours too: a click inside it must not read as
            // a click outside the overlay.
            if event.window !== self.window && event.window !== self.previewPanel {
                self.hide()
            }
            return event
        }
```

O monitor global não recebe `window`, então cliques em janelas do próprio app
não passam por ele — mas confirme isso na prática, porque é justamente onde a
suposição costuma falhar.

- [ ] **Step 3: Verificar à mão**

Abrir a overlay, abrir o painel, e confirmar: a overlay **continua aberta**; o
teclado continua indo para ela (as setas ainda navegam entre cards); clicar
dentro do painel não fecha nada; clicar fora dos dois fecha ambos.

- [ ] **Step 4: Decidir**

Funcionou → seguir para a Task 20. Não funcionou em duas tentativas → **parar
aqui**, registrar no `ROADMAP.md` o que foi tentado e o que aconteceu, e ir para
a verificação final. Reverter o painel descartável, mantendo a mudança dos
monitores apenas se ela for inofensiva por si.

---

## Task 20: O painel de preview

**Files:**
- Modify: `MyPasteApp/Window/ItemPreviewPanel.swift`
- Create: `MyPasteApp/Views/ItemPreviewView.swift`
- Modify: `MyPasteApp/Services/ItemActions.swift`
- Modify: `MyPasteApp/Views/ItemContextMenu.swift`

**Interfaces:**
- Consumes: o painel validado na Task 19.

- [ ] **Step 1: O conteúdo por tipo**

`MyPasteApp/Views/ItemPreviewView.swift`:

```swift
//
//  ItemPreviewView.swift
//  MyPasteApp
//

import AppKit
import SwiftUI

/// The contents of the preview panel.
///
/// Follows design-refs/03-preview-web.png and 04-preview-imagem.png: a title
/// bar of its own with a circular close button, the type beside it, and the
/// item filling the rest.
struct ItemPreviewView: View {
    let item: ClipboardItem
    let onClose: () -> Void

    var body: some View {
        VStack(spacing: 0) {
            header
            Divider()
            content
                .frame(maxWidth: .infinity, maxHeight: .infinity)
        }
        .frame(width: 520, height: 380)
    }

    private var header: some View {
        HStack(spacing: 8) {
            Button(action: onClose) {
                Image(systemName: "xmark.circle.fill")
                    .font(.system(size: 14))
                    .foregroundStyle(.secondary)
            }
            .buttonStyle(.plain)
            Text(typeLabel)
                .font(.system(size: 13, weight: .semibold))
            Spacer()
            if let footnote {
                Text(footnote)
                    .font(.system(size: 11))
                    .foregroundStyle(.secondary)
            }
        }
        .padding(.horizontal, 12)
        .padding(.vertical, 8)
    }

    @ViewBuilder
    private var content: some View {
        switch item.type {
        case .text, .url:
            // The whole thing, scrollable. This is the limitation the item
            // exists to fix: the card truncates at previewTextLength and eight
            // lines, so long text simply isn't readable in the app.
            ScrollView {
                Text(item.textContent ?? "")
                    .font(.system(size: 13))
                    .textSelection(.enabled)
                    .frame(maxWidth: .infinity, alignment: .leading)
                    .padding(12)
            }
        case .image:
            if let data = item.imageData, let img = NSImage(data: data) {
                ScrollView([.horizontal, .vertical]) {
                    Image(nsImage: img)
                        .resizable()
                        .scaledToFit()
                        .background(CheckerboardBackground())
                        .padding(12)
                }
            } else {
                Text(item.preview).font(.caption)
            }
        case .file:
            if let path = item.fileURLStrings?.first {
                VStack(spacing: 8) {
                    FilePreviewView(path: path)
                    Text(path)
                        .font(.system(size: 11, design: .monospaced))
                        .foregroundStyle(.secondary)
                        .textSelection(.enabled)
                }
                .padding(12)
            }
        }
    }

    private var typeLabel: String {
        switch item.type {
        case .text:  return "Texto"
        case .url:   return "URL"
        case .image: return "Imagem"
        case .file:  return "Arquivo"
        }
    }

    private var footnote: String? {
        switch item.type {
        case .text, .url:
            return "\(item.textContent?.count ?? 0) caracteres"
        case .image:
            guard let data = item.imageData, let img = NSImage(data: data) else { return nil }
            return "\(Int(img.size.width)) × \(Int(img.size.height))"
        case .file:
            return nil
        }
    }
}

/// The conventional way to show that an image has transparency.
struct CheckerboardBackground: View {
    var body: some View {
        Canvas { context, size in
            let square: CGFloat = 8
            let rows = Int(size.height / square) + 1
            let columns = Int(size.width / square) + 1
            for row in 0..<rows {
                for column in 0..<columns {
                    guard (row + column).isMultiple(of: 2) else { continue }
                    let rect = CGRect(x: CGFloat(column) * square,
                                      y: CGFloat(row) * square,
                                      width: square,
                                      height: square)
                    context.fill(Path(rect), with: .color(.gray.opacity(0.25)))
                }
            }
        }
    }
}
```

Itens de URL usam o mesmo texto completo dos itens de texto: o resumo visual já
está no card via `LinkPreviewView`, e `WKWebView` é o item 25.

- [ ] **Step 2: Ancorar ao card selecionado**

Posicionar o painel acima do card selecionado, com o bico apontando para ele
(`design-refs/03-preview-web.png`). O painel acompanha a mudança de seleção
enquanto estiver aberto.

- [ ] **Step 3: Acrescentar ao menu de contexto**

Entrada "Preview" com `␣` indicado à direita, no último grupo.

- [ ] **Step 4: Compilar e rodar a suíte**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS'
```

Esperado: PASS.

- [ ] **Step 5: Commit**

```
feat(preview): show the full item in an anchored panel

Roadmap item 19. Long text was truncated at previewTextLength and eight
lines, which meant it simply wasn't readable inside the app.
```

---

## Task 21: `Espaço` e a precedência do `Esc`

**Files:**
- Modify: `MyPasteApp/Views/OverlayView.swift`
- Create: `MyPasteAppTests/PreviewTriggerTests.swift`

**Interfaces:**
- Produces: `static func OverlayView.spaceOpensPreview(searchText:) -> Bool` e
  `static func OverlayView.escapeClosesPreview(isPreviewOpen:) -> Bool`.

- [ ] **Step 1: Escrever os testes**

`MyPasteAppTests/PreviewTriggerTests.swift`:

```swift
//
//  PreviewTriggerTests.swift
//  MyPasteAppTests
//

import Testing

@testable import MyPasteApp

@Suite("Preview trigger")
struct PreviewTriggerTests {
    @Test("Space opens the preview when the search is empty")
    func opensWithEmptySearch() {
        #expect(OverlayView.spaceOpensPreview(searchText: ""))
    }

    @Test("Space types normally once the search has text")
    func typesWhenSearchHasText() {
        // The search field always holds focus (OverlayView.onAppear), so Space
        // can't be claimed unconditionally. "foo bar" still works because by
        // the time the space is needed the field isn't empty.
        #expect(!OverlayView.spaceOpensPreview(searchText: "foo"))
    }

    @Test("A leading space in a search has no use")
    func leadingSpaceIsNotLost() {
        #expect(OverlayView.spaceOpensPreview(searchText: ""))
    }

    @Test("Escape closes the preview first")
    func escapeClosesPreviewBeforeOverlay() {
        // Convention: dismiss what's on top. Without this the user loses the
        // whole drawer while trying to close the panel over it.
        #expect(OverlayView.escapeClosesPreview(isPreviewOpen: true))
    }

    @Test("Escape closes the overlay when no preview is open")
    func escapeClosesOverlayOtherwise() {
        #expect(!OverlayView.escapeClosesPreview(isPreviewOpen: false))
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
set -o pipefail && NSUnbufferedIO=YES xcodebuild test \
  -project MyPasteApp.xcodeproj -scheme MyPasteApp \
  -configuration Debug -destination 'platform=macOS' \
  -only-testing:MyPasteAppTests/PreviewTriggerTests
```

Esperado: falha de compilação, `type 'OverlayView' has no member 'spaceOpensPreview'`.

- [ ] **Step 3: Implementar as duas regras**

```swift
    /// Whether Space should open the preview rather than type a space.
    ///
    /// The search field holds focus from `onAppear`, so Space can't simply be
    /// claimed by the overlay. A leading space in a search has no use, which
    /// makes an empty field the safe place to take the key; "foo bar" is
    /// unaffected because the field isn't empty by then.
    static func spaceOpensPreview(searchText: String) -> Bool {
        searchText.isEmpty
    }

    /// Whether Escape should close the preview instead of the overlay.
    ///
    /// Dismiss what's on top first, as the system does everywhere else.
    /// Without this, closing the panel takes the whole drawer with it.
    static func escapeClosesPreview(isPreviewOpen: Bool) -> Bool {
        isPreviewOpen
    }
```

Ligar as duas nos handlers: `onKeyPress(.space)` consulta a primeira, e
`onKeyPress(.escape)` (linha 87) consulta a segunda antes de chamar
`onDismiss()`.

- [ ] **Step 4: Rodar e confirmar que passa**

Mesmo comando do Step 2. Esperado: PASS, 5 testes.

- [ ] **Step 5: Verificação manual**

Com a busca vazia, `Espaço` abre o preview. Digitar "foo bar" na busca e
confirmar que o espaço no meio funciona. Com o painel aberto, as setas ainda
navegam entre cards e o painel acompanha. Primeiro `Esc` fecha o painel, segundo
fecha a overlay.

- [ ] **Step 6: Commit**

```
feat(preview): open with Space, close with Escape

Space is only claimed while the search is empty — the field holds focus from
the moment the overlay opens, so taking the key outright would break typing.
```

---

## Verificação manual final

**Antes do PR, e não antes disso a fase está pronta.** A suíte cobre lógica
pura; janelas, teclado, menus e barra de status não têm teste automatizado, e
nenhum agente consegue exercitar a GUI. Na Fase 1 foi exatamente isso que
deixou passar o teclado da overlay que nunca funcionou.

1. **Todas as preferências, uma a uma**, com dados anteriores à reconstrução —
   o roteiro da Task 8, Step 5. **É o item de maior risco da fase**
2. **Atalhos anunciados no menu** — cada atalho que o menu da barra de status e
   o menu de contexto exibem faz o que diz quando acionado pelo teclado
3. **Migração** — o app abre com o banco pré-existente e o histórico antigo está
   lá, inclusive imagens
4. **Texto rico ponta a ponta** — copiar formatado, colar formatado, `⇧↵` colar
   limpo, preferência global forçando limpo
5. **Editor** — `⌘E` foca o corpo, `⌘R` foca o rótulo, `⌘Z` desfaz, salvar
   atualiza o card, cancelar não altera nada
6. **`⌘N`** — item nasce com cor e ícone, fixado, e sobrevive a um `prune()`
7. **Preview** — overlay permanece aberta, setas continuam navegando, dois `Esc`
   para fechar tudo
8. **Retenção "Para Sempre"** — arrastar o slider até o fim, confirmar que itens
   antigos deixam de ser apagados, e que o limite de quantidade continua valendo

## Atualizações finais de documentação

- [ ] **`ROADMAP.md`** — marcar os itens 6 a 10 e 19 como concluídos, no formato
  que a Fase 1 usou (bloco `> ✅ **Feito.**` com o que foi decidido e o que
  ficou em aberto). Registrar as duas pendências de hotkey como resolvidas. Se o
  item 19 tiver saído no spike, registrar o que foi tentado
- [ ] **`DESIGN.md`** — marcar como feitas as mudanças derivadas 7 (menu de
  contexto), 8 (slider de retenção), 9 (sidebar) e 10 (descrição sob os
  toggles); e 13 (bico ancorado) se o item 19 tiver entrado
- [ ] **Pendências novas descobertas na fase** — acrescentar ao `ROADMAP.md`,
  como a Fase 1 fez com a sua seção de pendências herdadas
